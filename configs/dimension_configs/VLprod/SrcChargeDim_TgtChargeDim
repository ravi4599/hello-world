spark.src.config.id	SrcChargeDim 
spark.voyage.id	SC2002269NCR 
spark.tgt.env Prod
spark.target.config.id TgtChargeDim
spark.parent.id	SrcMxpCrewData-TgtMxpCrewData 
spark.target.ops.table.format org.apache.phoenix.spark
spark.target.ops.table.mode overwrite
spark.postgresql.database prodnbx
spark.target.enablerops_table ship.HBTB_INGESTION_METADATA
spark.target.zkurl P-VAL-DLK-ADA1.ahoy.virginvoyages.com:2181/hbase-secure
spark.table.type dim
spark.postgresql.url jdbc:postgresql://10.102.209.154:5432/
spark.postgresql.user nifiusr
spark.postgresql.password finiPRO@123
#spark.source.sql SELECT dp.voyage_id as voyage_id,gp.person_charge_id as charge_id,gp.batchtime,gp.partdate, dp.person_skey ,dp.person_type FROM (select * from (select * ,row_number() over ( partition by person_id order by person_lastChanged desc) as rn from shipdw.hvtb_parse_mxp_guest_person )guest_person where rn = 1) gp LEFT OUTER JOIN (select * from (select * ,row_number() over ( partition by person_id order by person_guid desc) as rn from shipdw.hvtb_mart_dim_person )person where rn = 1 ) dp  on (gp.person_id = dp.person_id)
spark.source.sql SELECT dp.voyage_id as voyage_id, cast(gp.charge_id_accounts as string) as charge_id, gp.batchtime as batchtime, gp.partdate, dp.person_skey,dp.person_type FROM (select * from (select charge_id_accounts ,booking_id,batchtime, part_date as partdate, row_number() over (partition by booking_id order by last_modified_date desc ) as rn from shipdw.hvtb_parse_mxp_sailor_crew_data)crew_data where crew_data.rn=1  )gp LEFT JOIN shipdw.hvtb_mart_dim_person dp on ( gp.booking_id = dp.booking_id)
spark.source.columns voyage_id,charge_id,batchtime,partdate,person_skey,person_type
spark.pond.allColumns charge_skey as pond_charge_skey,voyage_id as pond_voyage_id,charge_id as pond_charge_id,person_skey as pond_person_skey,person_type as pond_person_type, load_dt as pond_load_dt,upd_dt as pond_upd_dt,primaryhash as pond_primaryhash,md5_hash as pond_md5_hash
spark.surrogate_key.column charge_skey
spark.pond.database	shipdw 
spark.pond.table hvtb_mart_dim_charge
spark.target.schema	shipdw 
spark.postgresql.schema hive_schema_stg
spark.postgresql.tablename charge_dim
spark.source.primaryKeyColumns charge_id,voyage_id
spark.target.final_collist charge_skey,final_view.voyage_id,final_view.charge_id,final_view.person_skey,final_view.person_type,load_dt,upd_dt,primaryhash,md5_hash
spark.target.temp /data/apps/nbx/temp/hvtb_mart_dim_charge
spark.target.location /data/apps/nbx/hive/mart/hvtb_mart_dim_charge
spark.target.schema	Hive_schema_stg 
spark.postgres.tablename charge_dim
spark.scdtype type1
spark.tableType DIMENSION
spark.frameworkEnv ship
 
spark.ship.code VAL

