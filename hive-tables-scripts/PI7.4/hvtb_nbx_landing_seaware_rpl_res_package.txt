CREATE EXTERNAL TABLE `vv_db.hvtb_nbx_landing_sw_rpl_res_package`(

  `res_package_id` int,

  `start_date` timestamp,

  `end_date` timestamp,

  `effective_date` timestamp,

  `res_id` int,

  `guest_id` int,

  `package_type` string,

  `package_id` int,

  `is_priceable` string,

  `dep_ref_id` int,

  `arr_ref_id` int,

  `ship_code` string,

  `cabin_category` string,

  `cabin_category_berth` string,

  `cabin_seq_numbers` string,

  `package_class` string,

  `hotel_request_id` int,

  `res_addon_id` int,

  `dining_request_id` int,

  `car_req_leg_id` int,

  `shiproom_request_id` int,

  `package_status` string,

  `pps_card_owner_id` int,

  `comment_id` int,

  `is_no_show` string,

  `cab_ctg_upg_promo` string,

  `place_count` int,

  `parent_guest_id` int,

  `src_package_id` int,

  `src_dep_ref_id` int,

  `src_package_class` string,

  `src_arr_ref_id` int,

  `src_link_description` string,

  `res_product_id` int,

  `place_count_reserved` int,

  `source_code` string,

  `air_product_id` int,

  `transfer_request_id` int,

  `extra_guest_count` int,

  `res_tour_item_id` int,

  `ext_comp_id` int,

  `load_data_timestamp` timestamp)

ROW FORMAT SERDE

  'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'

WITH SERDEPROPERTIES (

  'field.delim'='\u0001',

  'line.delim'='\n',

  'serialization.format'='\u0001')

STORED AS INPUTFORMAT

  'org.apache.hadoop.mapred.TextInputFormat'

OUTPUTFORMAT

  'org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat'

LOCATION

  's3://vv-dev-emr-cluster/data/landing/spf/hvtb_nbx_landing_seaware_rpl_res_package'