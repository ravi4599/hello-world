
Drop view shipdw.hvtb_parse_vxp_ars_activity

CREATE EXTERNAL TABLE `shipdw`.`hvtb_parse_vxp_ars_activity`(`activityid` string, `activitydetailid` string, `activitylocationid` string, `bookingcloseduration` int, `isenable` boolean, `iswaitlistenable` boolean, `ispackageable` boolean, `isbookable` boolean, `activitygroupid` string, `portcode` string, `code` string, `currencycode` string, `ismultiplebookingallowed` boolean, `pricetype` string, `level` string, `isdeleted` boolean, `addeddate` timestamp, `lastmodifieddate` timestamp, `addedby` string, `lastmodifiedby` string, `activitygroupcode` string, `isvip` boolean, `isgiftable` boolean, `capacityid` string, `name` string, `accesstype` string, `decsription` string, `additionalinfo` string, `description` string, `cmsid` string, `maxage` int, `isadaaccessible` boolean, `minage` int, `voyageid` string, `batchtime` string)
PARTITIONED BY (`part_date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 's3://vv-prod-emr-cluster/data/parse/hvtb_parse_vxp_ars_activity'

