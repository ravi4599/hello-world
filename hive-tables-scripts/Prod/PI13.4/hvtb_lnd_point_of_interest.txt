CREATE EXTERNAL TABLE `vv_db.hvtb_lnd_point_of_interest`(
  `point_of_interest` string, 
  `address` string, 
  `city` string, 
  `zip` string, 
  `filename` string, 
  `batchtime` timestamp, 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/landing/transfer/hvtb_lnd_point_of_interest';

create view vv_db.hvtb_parse_point_of_interest as select * from vv_db.hvtb_lnd_point_of_interest
