CREATE EXTERNAL TABLE vv_db.hvtb_mart_fact_marketing_clm_status(
  ga360_fullvisitorid string, 
  client_id int, 
  lead_id string, 
  clm_stage string, 
  clm_sub_stage string, 
  clm_as_of_datetime timestamp, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
PARTITIONED BY ( 
  part_dt date)
STORED AS PARQUET
LOCATION
  's3://vv-prod-emr-cluster/data/mart/hvtb_mart_fact_marketing_clm_status' ;
