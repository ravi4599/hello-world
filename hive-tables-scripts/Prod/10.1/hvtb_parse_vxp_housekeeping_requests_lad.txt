CREATE EXTERNAL TABLE `shipdw.hvtb_parse_vxp_housekeeping_requests_lad`(
  `id` bigint, 
  `public_id` string, 
  `place_id` int, 
  `description` string, 
  `created` timestamp, 
  `priority` int, 
  `request_type` int, 
  `place_deck_id` int, 
  `place_section_id` int, 
  `assigned` string, 
  `created_by` string, 
  `loyalty` int, 
  `started_at` timestamp, 
  `completed_at` timestamp, 
  `assigned_at` timestamp, 
  `at_risk_at` timestamp, 
  `overdue_at` timestamp, 
  `cancelled_at` timestamp, 
  `come_back_later_at` timestamp, 
  `come_back_later_by` string, 
  `deleted` timestamp, 
  `guests` string, 
  `ts` timestamp, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/mart/hvtb_parse_vxp_housekeeping_requests_lad'
