hdfs dfs -rm -r s3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_itinerary

emrfs delete s3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_itinerary

drop table shipdw.hvtb_mart_dim_itinerary

CREATE EXTERNAL TABLE `shipdw`.`hvtb_mart_dim_itinerary`(`itinerary_skey` bigint, `voyage_id` string, `voyage_number` string, `itinerary_id` string, `voyage_itinerary_guid` string, `itinerary_name` string, `itinerary_day` int, `itinerary_embark_dt` timestamp, `itinerary_debark_dt` timestamp, `itinerary_embark_port_code` string, `itinerary_debark_port_code` string, `gangway_open_time` timestamp, `gangway_close_time` timestamp, `itinerary_departure_time` timestamp, `itinerary_arrival_time` timestamp, `itinerary_isseaDay` boolean, `itinerary_day_date` timestamp,`src_active_flag` boolean,`src_deleted_flag` boolean ,`load_dt` timestamp, `upd_dt` timestamp, `primaryhash` string, `md5_hash` string)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_itinerary'