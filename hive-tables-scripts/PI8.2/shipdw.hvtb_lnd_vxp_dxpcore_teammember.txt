
CREATE EXTERNAL TABLE `shipdw.hvtb_lnd_vxp_dxpcore_teammember`(
  `teammemberid` string, 
  `teammembertypeid` string, 
  `departmentkey` string, 
  `teammembernumber` string, 
  `sourceid` string, 
  `venuecode` string, 
  `useraccountid` string, 
  `titlecode` string, 
  `firstname` string, 
  `middlename` string, 
  `lastname` string, 
  `nickname` string, 
  `preferredname` string, 
  `suffixcode` string, 
  `stateroom` string, 
  `birthdate` date, 
  `citizenshipcountrycode` string, 
  `gender` string, 
  `maritalstatus` string, 
  `email` string, 
  `alternateemail` string, 
  `joiningdate` date, 
  `embarkdate` timestamp, 
  `debarkdate` timestamp, 
  `photomediaitemid` string, 
  `photomodifieddate` timestamp, 
  `isdeleted` boolean, 
  `addeddate` timestamp, 
  `addedby` string, 
  `lastmodifieddate` timestamp, 
  `lastmodifiedby` string, 
  `propertyid` string, 
  `gendercode` string, 
  `preferredlanguage` string, 
  `departmentcode` string, 
  `embarkport` string, 
  `debarkport` string, 
  `profileline` string, 
  `medicalinformation` string, 
  `astrologicalsigncode` string, 
  `supervisorid` string, 
  `mentorid` string, 
  `birthcountrycode` string, 
  `maritalstatuscode` string, 
  `designation` string, 
  `actualdebarkdate` timestamp, 
  `pmsid` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-dev-emr-cluster/data/landing/hvtb_lnd_vxp_dxpcore_teammember'

