spark.sql("""drop table vv_db.hvtb_parse_rts_postcruiseinfo""")
spark.sql("""CREATE EXTERNAL TABLE vv_db.hvtb_parse_rts_postcruiseinfo(
  `guestid` string, 
  `reservationnumber` string, 
  `postcruiseinfo_flightnumber` string, 
  `postcruiseinfo_flyingoutplancode` string, 
  `postcruiseinfo_isopted` boolean,
  `postcruiseinfo_departureTime` string, 
  `postcruiseinfo_airlinecode` string,  
  `n` string, 
  `postcruiseinfo_prepostcruiseid` string, 
  `ts` timestamp, 
  `batchtime` timestamp, 
  `voyageid` string, 
  `shipcode` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/parse/mxp/hvtb_parse_rts_postcruiseinfo'""")
  
  
 spark.sql("""drop table vv_db.hvtb_parse_rts_precruiseinfo """)
spark.sql("""CREATE EXTERNAL TABLE vv_db.hvtb_parse_rts_precruiseinfo(
  `guestid` string, 
  `reservationnumber` string, 
  `precruiseinfo_transportationtypecode` string, 
  `precruiseinfo_flightnumber` string, 
  `precruiseinfo_arrivaltime` string, 
  `precruiseinfo_departuretime` string,
  `precruiseinfo_airlinecode` string,  
  `n` string, 
  `precruiseinfo_prepostcruiseid` string, 
  `ts` timestamp, 
  `batchtime` timestamp, 
  `voyageid` string, 
  `shipcode` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/parse/mxp/hvtb_parse_rts_precruiseinfo'""")

spark.sql("""drop table vv_db.hvtb_parse_rts_checkinguestdetails""")

  
spark.sql("""CREATE EXTERNAL TABLE vv_db.hvtb_parse_rts_checkinguestdetails(
  `guestid` string, 
  `reservationnumber` string, 
  `reservationguestid` string, 
  `reservationembarkdate` string, 
  `reservationdebarkdate` string, 
  `statuscode` string, 
  `paymentstatuscode` string, 
  `guestmoderationstatus` string, 
  `guestoveralldocumentstatus` string, 
  `isguestonboard` boolean, 
  `isvalidated` boolean, 
  `isassociatedflightbooking` boolean, 
  `isitineraryrulesapplied` boolean, 
  `checkinguestdetails_security_photo` string, 
  `health_questions` string, 
  `checkinguestdetails_emergency_contact` string, 
  `checkinguestdetails_travel_documents` string, 
  `post_voyage` string, 
  `checkinguestdetails_embarkation_slot` string, 
  `checkinguestdetails_voyage_contract` string, 
  `checkinguestdetails_payment_method` string, 
  `checkinguestdetails_pregnancy` string, 
  `percentagecheckinstatus_security_photo` int, 
  `percentagecheckinstatus_emergency_contact` int, 
  `percentagecheckinstatus_travel_documents` int, 
  `percentagecheckinstatus_embarkation_slot` int, 
  `percentagecheckinstatus_payment_method` int, 
  `percentagecheckinstatus_voyage_contract` int, 
  `checkinguestdetails_totalpercentagecheckinstatus` int,
  `lastmodificationtime` timestamp,  
  `n` string, 
  `precruiseinfo_prepostcruiseid` string, 
  `postcruiseinfo_prepostcruiseid` string, 
  `ts` timestamp, 
  `batchtime` timestamp, 
  `voyageid` string, 
  `shipcode` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/parse/mxp/hvtb_parse_rts_checkinguestdetails'""")
  
  
  spark.sql("""drop table vv_db.hvtb_mart_dim_seaware_guest_rts""")
 spark.sql("""CREATE EXTERNAL TABLE vv_db.hvtb_mart_dim_seaware_guest_rts(
  `rts_id` bigint, 
  `vxp_guest_id` string, 
  `src_res_id` int, 
  `src_guest_id` int, 
  `client_id` double, 
  `rts_transfer_response` string, 
  `arrival_transportation_mode` string, 
  `arrival_booking_source` string, 
  `arrival_airline_code` string,
  `arrival_flight_number` string, 
  `arrival_transportation_date` date, 
  `arrival_transportation_time` string, 
  `departure_transportation_mode` string, 
  `departure_booking_source` string, 
  `departure_airline_code` string,
  `departure_flight_number` string, 
  `departure_transportation_date` date, 
  `departure_transportation_time` string, 
  `parking_at_port_flg` char(1), 
  `statuscode` varchar(20), 
  `paymentstatuscode` varchar(20), 
  `guestmoderationstatus` varchar(20), 
  `guestoveralldocumentstatus` varchar(20), 
  `isguestonboard` boolean, 
  `isvalidated` boolean, 
  `isassociatedflightbooking` boolean, 
  `isitineraryrulesapplied` boolean, 
  `security_photo_checkin_status` varchar(20), 
  `health_questions_checkin_status` varchar(20), 
  `emergency_contact_checkin_status` varchar(20), 
  `travel_documents_checkin_status` varchar(20), 
  `post_voyage_checkin_status` varchar(20), 
  `embarkation_slot_checkin_status` varchar(20), 
  `voyage_contract_checkin_status` varchar(20), 
  `payment_method_checkin_status` varchar(20), 
  `pregnency_checkin_status` varchar(20), 
  `security_photo_checkin_percentage` int, 
  `emergency_contact_checkin_percentage` int, 
  `travel_documents_checkin_percentage` int, 
  `embarkation_slot_checkin_percentage` int, 
  `payment_method_checkin_percentage` int, 
  `voyage_contract_checkin_percentage` int, 
  `checkin_status_total_percentage` int, 
  `reservation_last_modification_time` timestamp,
  `voyageid` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_seaware_guest_rts'""")
  