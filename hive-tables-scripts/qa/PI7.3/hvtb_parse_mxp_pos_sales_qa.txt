Drop table shipdw.hvtb_parse_mxp_pos_sales;

CREATE EXTERNAL TABLE `shipdw.hvtb_parse_mxp_pos_sales`(
	  `sales_id` bigint, 
	  `sales_guid` string, 
	  `sales_businessunitid` int, 
	  `sales_shipcode` string, 
	  `sales_voyagenumber` string,
	  `sales_receiptnr` int, 
	  `sales_operatornr` int, 
	  `sales_accountnr` int, 
	  `sales_chargeid` string, 
	  `sales_date` string, 
	  `sales_time` string, 
	  `sales_outletid` int, 
	  `sales_outletname` string, 
	  `sales_checkstatus` string, 
	  `sales_synchronized` string, 
	  `sales_created` string, 
	  `sales_voidedparent` int, 
	  `sales_voidreason` string, 
	  `voyageid` string, 
	  `batchtime` timestamp)
	PARTITIONED BY ( 
	  `part_date` date)
	LOCATION
	  's3a://vv-qa-emr-cluster/data/parse/mxp/hvtb_parse_mxp_pos_sales'