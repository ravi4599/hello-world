CREATE EXTERNAL TABLE `vv_db_mig.hvtb_nbx_core_sw_agency_dim`(
  `agency_id` int, 
  `rec_start_dttm` timestamp, 
  `rec_end_dttm` timestamp, 
  `src_agency_id` int, 
  `agency_name` string, 
  `agency_code` string, 
  `agency_market` string, 
  `agency_class` string, 
  `agency_sales_region` string, 
  `agency_sales_district` string, 
  `agency_country` string, 
  `agency_state` string, 
  `agency_zip` string,  
  `etl_ld_status` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string, 
  `scd_hash` string)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/mart/seaware_replica/hvtb_nbx_core_sw_agency_dim'