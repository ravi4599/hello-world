CREATE EXTERNAL TABLE `shipdw.hvtb_parse_ars_booking`(
  `reservationnumber` string, 
  `personid` string, 
  `activitycode` string, 
  `externalbookingid` string, 
  `persontypecode` string, 
  `ischeckedin` boolean, 
  `activitypersonbookingid` string, 
  `activitybookingid` string, 
  `activityslotcode` string, 
  `activitygroupcode` string, 
  `startdatetime` string, 
  `enddatetime` string, 
  `bookedbypersonid` string, 
  `bookedbypersontypecode` string, 
  `bookingamountpaid` double, 
  `bookingamountpending` double, 
  `bookinglinkid` string, 
  `cancelledbypersonid` string, 
  `cancelledbypersontypecode` string, 
  `createdbypersontype` string, 
  `discountamount` double, 
  `discountunit` string, 
  `giftviewed` boolean, 
  `gifted` boolean, 
  `modifiedbypersontype` string, 
  `operationtype` string, 
  `payeepersonid` string, 
  `payeepersontypecode` string, 
  `paymentstatus` string, 
  `percentdiscountgiven` double, 
  `refundamount` double, 
  `refundamountpaid` double, 
  `refundamountpending` double, 
  `status` string, 
  `waiverstatus` string, 
  `waivercode` string, 
  `waiversignedtime` string, 
  `waiverversion` string, 
  `cancellationreason` string, 
  `cancellationreasonid` string, 
  `type` string, 
  `transactionid` string, 
  `transactionamount` double, 
  `bookingconstraint` string, 
  `batchtime` timestamp, 
  `voyageid` string, 
  `ts` string, 
  `voyagenumber` string)
PARTITIONED BY ( 
  `partdate` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/parse/ars/hvtb_parse_ars_booking'