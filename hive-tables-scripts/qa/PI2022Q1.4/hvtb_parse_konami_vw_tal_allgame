CREATE EXTERNAL TABLE `shipdw`.`hvtb_parse_konami_vw_tal_allgame`(`devcatid` decimal(3,0), `devsubcatid` decimal(3,0), `masterprom` string, `gameprom` string, `gamecode` string, `paytablecode` string, `gameindex` decimal(10,0), `gamename` string, `theholdpct` decimal(9,6), `dph` decimal(9,6), `lastmoddate` timestamp, `lastmodby` string, `devgamename` string, `devpaytablecode` string, `devgamecrc` decimal(10,0), `devtheholdpct` decimal(6,0), `maxcoin` decimal(6,0), `uploaded` decimal(1,0), `devpaytablecode2` string, `isbettype` decimal(1,0), `gameid` decimal(10,0), `mfrid` decimal(10,0), `voyageid` string, `batchtime` timestamp, `shipcode` string)
PARTITIONED BY (`part_date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 's3://vv-qa-emr-cluster/data/parse/hvtb_parse_konami_vw_tal_allgame'
