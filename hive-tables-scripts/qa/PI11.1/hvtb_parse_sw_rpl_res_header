CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_header`(
  `res_id` int,
  `res_init_date` timestamp,
  `res_guest_count` int,
  `agency_id` int,
  `agent_id` int,
  `cabin_category` string,
  `res_status` string,
  `probability` decimal(7,4),
  `group_id` int,
  `delegate_header_id` int,
  `comments` string,
  `crs_status` string,
  `dpms_status` string,
  `termination_completed` string,
  `ship_code` string,
  `package_type` string,
  `sail_date_from` timestamp,
  `sail_date_to` timestamp,
  `rel_day_from` int,
  `rel_day_to` int,
  `source_code` string,
  `alt_res_id` string,
  `sec_agency_id` int,
  `office_code` string,
  `currency_rate` decimal(10,5),
  `currency_code` string,
  `rh_entity_type` string,
  `cancellation_case` int,
  `office_location` string,
  `dep_ref_id` int,
  `arr_ref_id` int,
  `sec_agent_id` int,
  `company_id` int,
  `company_agent_id` int,
  `contact_id` int,
  `res_type` string,
  `parent_res_id` int,
  `ownership_code` string,
  `vt_package_id` int,
  `res_name` string,
  `last_updated_at` timestamp,
  `is_prob_manual` string,
  `res_mode` string,
  `customer_reference` string,
  `agreement_agency_id` int,
  `was_imo_controlled` string,
  `consignee_id` int,
  `consignee` string,
  `allotment_header_id` int,
  `needs_control_before_invoice` string,
  `consignee_client_id` int,
  `cargo_dev_type` string,
  `cargo_dev_type_ext` string,
  `cargo_dev_info` string,
  `cargo_dev_status` string,
  `seller_id` int,
  `seller_vat_id` int,
  `buyer_vat_id` int,
  `allow_invoicing` string,
  `buyer_id` int,
  `language_code` string,
  `last_payment_ext_date` timestamp,
  `cab_distrib_bypass_eligib_rule` string,
  `alt_grouping` string,
  `has_external_components` string,
  `boarding_zone` string,
  `shipper_id` int,
  `consider_paid` string,
  `cargo_by_shipper_order` string,
  `notify_agency_id` int,
  `notify_client_id` int,
  `notify_message` string,
  `cargo_by_consignee_order` string,
  `extra_agency_id` int,
  `ext_requester_client_id` int,
  `ext_requester_agency_id` int,
  `hold_updates_to_air_system` string,
  `res_version` int,
  `trw_referral_code` string,
  `offer_code` string,
  `payer_client_id` int,
  `address_guest_as` string,
  `op` string,
  `ts_ms` timestamp,
  `shipcode` string,
  `voyageid` string,
  `batchtime` timestamp)
PARTITIONED BY (
  `part_date` date)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_header'