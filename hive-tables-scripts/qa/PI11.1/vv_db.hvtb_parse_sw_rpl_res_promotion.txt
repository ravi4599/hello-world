CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_promotion`(
  	`res_promo_id` int,
	`res_id` int,
	`guest_id` int,
	`promo_code` string,
	`is_excluded` string,
	`is_active` string,
	`res_package_id` int,
	`is_manual` string,
	`promo_value` decimal(10,2),
	`promo_value_type` string,
  	`op` string,
  	`ts_ms` timestamp,
  	`shipcode` string,
  	`voyageid` string,
  	`batchtime` timestamp)
PARTITIONED BY (
  `part_date` date)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_promotion'
