CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_payment_due_item`(
  `res_payment_due_item_id` int,
  `pmnt_due_item_type` string,
  `guest_id` int,
  `res_id` int,
  `due_date` timestamp,
  `grace_period` int,
  `amount` decimal(10,2),
  `expiration_date` timestamp,
  `designated_payment_code` string,
  `is_paid` string,
  `is_paid_set_manually` string,
  `manual_funds_distr` string,
  `op` string,
  `ts_ms` timestamp,
  `shipcode` string,
  `voyageid` string,
  `batchtime` timestamp)
PARTITIONED BY (
  `part_date` date)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_payment_due_item'