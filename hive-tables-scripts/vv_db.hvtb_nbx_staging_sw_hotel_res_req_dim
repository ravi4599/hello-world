CREATE EXTERNAL TABLE `vv_db.hvtb_nbx_staging_sw_hotel_res_req_dim`(
  `hotel_res_req_id` int, 
  `src_hotel_res_req_id` int, 
  `src_res_id` int, 
  `src_guest_id` int, 
  `request_type` string, 
  `start_date` timestamp, 
  `end_date` timestamp, 
  `city_code` varchar(10), 
  `hotel_id` int, 
  `hotel_category` string, 
  `room_seq_number` int, 
  `room_category` string, 
  `room_type` string, 
  `occupancy` int, 
  `hotel_space_type` string, 
  `allocation_id` int, 
  `inv_result` string, 
  `is_land_component` string, 
  `hotel_bed_type` string, 
  `effective_date` timestamp, 
  `etl_ld_status` string, 
  `etl_ld_dt` timestamp, 
  `etl_upd_dt` timestamp, 
  `md5_dim` string)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/staging/seaware/hvtb_nbx_core_sw_hotel_res_req_dim'
;
