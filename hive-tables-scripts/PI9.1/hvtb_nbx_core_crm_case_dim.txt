CREATE EXTERNAL TABLE `vv_db.hvtb_nbx_core_crm_case_dim`(
  `case_id` int, 
  `rec_start_dttm` timestamp, 
  `rec_end_dttm` timestamp, 
  `id` string, 
  `contactid` string, 
  `accountid` string, 
  `type` string, 
  `casenumber` string, 
  `reason` string, 
  `createddate` timestamp, 
  `closeddate` timestamp, 
  `caseownerid__c` string, 
  `casesubreason__c` string, 
  `case_resolution__c` string, 
  `case_sub_resolution__c` string, 
  `complaint_type__c` string, 
  `origin` string, 
  `subject` string, 
  `complaint__c` boolean, 
  `request_assistance_queue__c` string, 
  `request_assistance_reason__c` string, 
  `external_case_resolution__c` string, 
  `external_case_sub_resolution__c` string, 
  `case_activity_counter__c` double, 
  `reopened__c` boolean, 
  `escalated__c` boolean, 
  `total_case_resolution_time__c` double, 
  `external_escalation__c` boolean, 
  `first_contact_resolution_chat__c` boolean, 
  `status` string, 
  `ownerid` string, 
  `assetid` string, 
  `booked_activities__c` string, 
  `booking_payments__c` string, 
  `businesshoursid` string, 
  `caseaudience__c` string, 
  `category__c` string, 
  `count_child_cases__c` int, 
  `count_closed_cases__c` int, 
  `createdbyid` string, 
  `description` string, 
  `entitlementid` string, 
  `escalation_reason__c` string, 
  `isclosed` boolean, 
  `isclosedoncreate` boolean, 
  `isdeleted` boolean, 
  `isescalated` boolean, 
  `isstopped` boolean, 
  `last_interaction_date__c` timestamp, 
  `lastmodifiedbyid` string, 
  `lastmodifieddate` timestamp, 
  `link__c` string, 
  `milestonestatus` string, 
  `navigatorname__c` string, 
  `parentid` string, 
  `priority` string, 
  `productid` string, 
  `recordtypeid` string, 
  `reservation_number__c` string, 
  `sailor_email__c` string, 
  `sailor_name__c` string, 
  `ship__c` string, 
  `slaexitdate` timestamp, 
  `slastartdate` timestamp, 
  `stopstartdate` timestamp, 
  `sub_category__c` string, 
  `suppliedcompany` string, 
  `suppliedemail` string, 
  `suppliedname` string, 
  `suppliedphone` string, 
  `systemmodstamp` timestamp, 
  `transferred_to__c` string, 
  `voyage__c` string, 
  `webfname__c` string, 
  `weblname__c` string, 
  `case_owner_queue__c` string, 
  `booking_flow__c` string, 
  `case_owner__c` string, 
  `escalated_flag__c` string, 
  `reopened_count__c` int, 
  `first_caseownerqueue__c` string, 
  `md5_dim` string, 
  `etl_ld_status` string, 
  `etl_ld_dt` timestamp, 
  `etl_upd_dt` timestamp)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-dev-emr-cluster/data/core/crm_kpi/hvtb_nbx_core_crm_case_dim'
;
