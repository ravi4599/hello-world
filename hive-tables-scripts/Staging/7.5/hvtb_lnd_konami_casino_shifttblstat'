
CREATE EXTERNAL TABLE `shipdw.hvtb_lnd_konami_casino_shifttblstat`(
  `batchinstanceid` string, 
  `voyageid` string, 
  `batchtime` timestamp, 
  `message` string)
PARTITIONED BY ( 
  `part_date` date)
stored as parquet 
LOCATION
  's3://vv-staging-emr-cluster/data/landing/hvtb_lnd_konami_casino_shifttblstat'

