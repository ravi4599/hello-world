CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_invoice_item`(
  	`res_invoice_item_id` decimal(10),
	`res_id` int,
	`guest_id` int,
	`res_package_id` int,
	`invoice_item_type` string,
	`price_per_unit` decimal(10,4),
	`n_of_units` decimal(11,3),
	`percent` decimal(10,2),
	`amount` decimal(10,2),
	`invoice_item_subtype` string,
	`invoice_item_subtype2` string,
	`invoice_item_subtype3` string,
	`price_program` string,
	`hotel_request_id` int,
	`commission_percent` decimal(10,2),
	`price_area` string,
	`extended_info` string,
	`adjustment_origin` string,
	`res_product_id` int,
	`invoice_hdr_id` decimal(10),
	`client_id` int,
	`guest_seqn` int,
	`amount_in_hdr_curr` decimal(10,2),
  	`op` string,
  	`ts_ms` timestamp,
  	`shipcode` string,
  	`voyageid` string,
  	`batchtime` timestamp)
PARTITIONED BY (
  `part_date` date)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-staging-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_invoice_item'