CREATE EXTERNAL TABLE `vv_db_mig.hvtb_nbx_parse_sw_rpl_country`(
  `country_id` int, 
  `country_code` string, 
  `country_name` string, 
  `country_code2` string, 
  `country_iso_id` int, 
  `intl_code` int, 
  `phone_input_mask` string, 
  `postal_code_mask` string, 
  `postal_code_provided` string, 
  `postal_code_complete` string, 
  `postal_symbols_num_used` int, 
  `agency_id` int, 
  `country_code3` string, 
  op string,
ts_ms timestamp,
voyageid string,
Batchtime timestamp) partitioned by (Part_Date Date) STORED AS PARQUET
LOCATION
's3://vv-staging-emr-cluster/data/parse/seaware/hvtb_nbx_parse_sw_rpl_country'