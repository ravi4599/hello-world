DROP TABLE IF EXISTS `vv_db.processcontroller`;
CREATE TABLE `vv_db.processcontroller`(
  `processname` string, 
  `variablename` string, 
  `value` string
  )
ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t'
STORED AS TEXTFILE
LOCATION 's3://vv-dev-emr-cluster/data/hivetables/vv_db/processcontroller';
SELECT * FROM `vv_db.processcontroller`;
