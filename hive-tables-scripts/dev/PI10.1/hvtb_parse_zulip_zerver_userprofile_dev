
CREATE EXTERNAL TABLE `shipdw`.`hvtb_parse_zulip_zerver_userprofile`(`id` int, `password` string, `last_login` timestamp, `is_superuser` boolean, `email` string, `is_staff` boolean, `is_active` boolean, `is_bot` boolean, `date_joined` timestamp, `is_mirror_dummy` boolean, `full_name` string, `short_name` string, `pointer` int, `last_pointer_updater` string, `api_key` string, `enable_stream_desktop_notifications` boolean, `enable_stream_audible_notifications` boolean, `enable_desktop_notifications` boolean, `enable_sounds` boolean, `enable_offline_email_notifications` boolean, `enable_offline_push_notifications` boolean, `enable_digest_emails` boolean, `last_reminder` timestamp, `rate_limits` string, `default_all_public_streams` boolean, `enter_sends` boolean, `twenty_four_hour_time` boolean, `avatar_source` string, `tutorial_status` string, `onboarding_steps` string, `alert_words` string, `bot_owner_id` int, `default_events_register_stream_id` int, `default_sending_stream_id` int, `realm_id` int, `left_side_userlist` boolean, `is_api_super_user` boolean, `is_realm_admin` boolean, `bot_type` int, `default_language` boolean, `tos_version` string, `enable_online_push_notifications` boolean, `pm_content_in_desktop_notifications` boolean, `avatar_version` int, `timezone` string, `emojiset` string, `last_active_message_id` int, `long_term_idle` boolean, `high_contrast_mode` boolean, `enable_stream_push_notifications` boolean, `night_mode` boolean, `enable_stream_email_notifications` boolean, `realm_name_in_notifications` boolean, `translate_emoticons` boolean, `message_content_in_email_notifications` boolean, `is_guest` boolean, `dense_mode` boolean, `delivery_email` string, `starred_message_counts` boolean, `is_billing_admin` boolean, `enable_login_emails` boolean, `notification_sound` string, `fluid_layout_width` boolean, `demote_inactive_streams` int, `avatar_hash` string, `desktop_icon_count_display` int, `iam_user_id` string, `VoyageId` string, `BatchTime` timestamp)
PARTITIONED BY (`part_date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 's3://vv-dev-emr-cluster/data/parse/hvtb_parse_zulip_zerver_userprofile'