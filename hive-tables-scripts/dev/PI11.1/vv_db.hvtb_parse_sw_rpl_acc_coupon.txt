CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_acc_coupon`(
	`COUPON_ID` int,
	`VALID_FROM` timestamp,
	`VALID_TO` timestamp ,
	`AMOUNT` decimal(15,2) ,
	`AMOUNT_LEFT` decimal(15,2),
	`IS_ACTIVE` string,
	`COMMENTS` string,
	`COUPON_CLASS` string,
	`OFFICE_CODE` string,
	`RES_ID` int,
	`GEN_LINK_ID` int,
	`ENTITY_TYPE` string,
	`ENTITY_ID` int,
	`IS_USED` string,
	`TEMP_RES_ID` int,
	`PAP_CODE` string,
	`GRANTED_TO_CLIENT_ID` int,
	`CURRENCY_CODE` string,
	`FREQUENT_PGM_POINTS` decimal(10,2),
	`REASON_CODE` string,
	`EFFECTIVE_FROM` timestamp,
	`EFFECTIVE_TO` timestamp,
	`RES_ADDON_ID` int,
	`DAYS_AFTER_FIRST_USE_EFF` int,
	`NO_SHOW_DEP_REF_ID` int,
	`GL_EXPIRATION_DATE` timestamp,
	`GL_EXPIRATION_ERROR` string,
	`GL_CREATION_DATE` timestamp,
	`GL_CREATION_ERROR` string,
        `LINKED_TRANS_ID` int,
	`USER_DEFINED_COUPON_ID` string,
	`COUPON_CATEGORY` string,
	`CHARGE_CODE` string,
  `op` string,
  `ts_ms` timestamp,
  `shipcode` string,
  `voyageid` string,
  `batchtime` timestamp)
PARTITIONED BY (
  `part_date` date)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-dev-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_acc_coupon'