
CREATE EXTERNAL TABLE `shipdw.hvtb_parse_vxp_ars_activitybooking`(
  `activitybookingid` string, 
  `bookinglinkid` string, 
  `externalbookingid` string, 
  `activitycode` string, 
  `status` string, 
  `cancellationreasonid` string, 
  `paymentstatus` string, 
  `bookingamountpaid` decimal(10,2), 
  `bookingamountpending` decimal(10,2), 
  `refundamountpending` decimal(10,2), 
  `refundamountpaid` decimal(10,2), 
  `waiverstatus` string, 
  `refundreasonid` string, 
  `notes` string, 
  `addtionalnotes` string, 
  `reservationnumber` string, 
  `activityslotcode` string, 
  `startdatetime` timestamp, 
  `enddatetime` timestamp, 
  `payeepersontypecode` string, 
  `payeepersonid` string, 
  `bookedbypersontypecode` string, 
  `bookedbypersonid` string, 
  `transactionid` string, 
  `isdeleted` boolean, 
  `addeddate` timestamp, 
  `lastmodifieddate` timestamp, 
  `addedby` string, 
  `lastmodifiedby` string, 
  `isgifted` boolean, 
  `isgiftviewed` boolean, 
  `waiverversion` string, 
  `waivercode` string, 
  `waiversignedtime` timestamp, 
  `cancelledbypersontypecode` string, 
  `cancelledbypersonid` string, 
  `cancellationreason` string, 
  `activitygroupcode` string, 
  `notesid` string, 
  `discountamount` decimal(10,2), 
  `discountunit` string, 
  `discountreason` string, 
  `discountreasonid` string, 
  `voyagenumber` string, 
  `type` string, 
  `bookingconstraint` string, 
  `transactionamount` decimal(10,2),
  `currencycode` string,
  `currencyrate` decimal(10,2)  
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-dev-emr-cluster/data/parse/hvtb_parse_vxp_ars_activitybooking'