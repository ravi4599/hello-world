CREATE EXTERNAL TABLE vv_db.hvtb_parse_sw_rpl_group_delegate_guest (
  `delegate_guest_id` int,
  `delegate_header_id` int,
  `guest_seqn` int,
  `guest_id` int,
  `guest_type` string,
  `guest_age` int,
  `client_id` int,
  `dining_type` string,
  `insurance` string,
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-dev-emr-cluster/data/parse/seaware/hvtb_parse_sw_rpl_group_delegate_guest'