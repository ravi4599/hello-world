  CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_protected_commission`(
  `record_id` int, 
  `res_id` int, 
  `commiss_source` string, 
  `commiss_code` string, 
  `percent` decimal(10,2), 
  `amount` decimal(10,2), 
  `commiss_fare` decimal(10,2), 
  `is_manual_adjustment` string, 
  `is_lower_limit` string, 
  `is_upper_limit` string, 
  `agency_id` int, 
  `payout_date` timestamp, 
  `gl_prototype_id` int, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_protected_commission';