
CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_ship_room_request`(
  `request_id` int, 
  `owner_type` string, 
  `owner_id` int, 
  `effective_date` timestamp, 
  `responsible_guest` int, 
  `inventory_request_type` string, 
  `ship_code` string, 
  `facility_type` string, 
  `layout_code` string, 
  `usage_start` timestamp, 
  `usage_duration` int, 
  `party_size` int, 
  `allocation_id` int, 
  `external_guest_count` int, 
  `attachment_id` int, 
  `comments` string, 
  `notes` string, 
  `quantity` int, 
  `ownership_mode` string, 
  `no_show_count` int, 
  `is_sail_canceled` string, 
  `ext_comp_id` int, 
  `facility_subtype` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_ship_room_request'
  
  