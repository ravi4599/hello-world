  CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_passenger`(
  `pax_id` int, 
  `res_id` int, 
  `guest_id` int, 
  `client_id` int, 
  `last_name` string, 
  `first_name` string, 
  `middle_name` string, 
  `birth_date` timestamp, 
  `origin_info` string, 
  `created_at` timestamp, 
  `comments` string, 
  `title` string, 
  `suffix` string, 
  `gender` string, 
  `base_pax_id` int, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_passenger';