  CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_hotel_inv_allocation`(
  `allocation_id` int, 
  `allocation_owner_type` string, 
  `allocation_owner_id` int, 
  `allocation_timestamp` timestamp, 
  `operator_id` string, 
  `inventory_request_type` string, 
  `depends_on_id` int, 
  `seq_number` int, 
  `probability` decimal(7,4), 
  `inventory_result_type` string, 
  `hotel_room_category` string, 
  `hotel_category` string, 
  `hotel_id` int, 
  `check_in_date` timestamp, 
  `check_out_date` timestamp, 
  `hotel_room_type` string, 
  `priority` int, 
  `occupancy` int, 
  `decline_reason` int, 
  `release_timestamp` timestamp, 
  `ship_code` string, 
  `city_code` string, 
  `wtl_owner_flag` string, 
  `dlgt_res_id` int, 
  `is_dlgt_obj` string, 
  `hotel_space_type` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
 'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_hotel_inv_allocation';