CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_am_code`(
  `amenity_code` string, 
  `record_id` int, 
  `am_category` string, 
  `amenity_type` string, 
  `provider_code` string, 
  `amenity_name` string, 
  `comments` string, 
  `use_tax` string, 
  `subcode_mandatory` string, 
  `mandatory_before_dlv` string, 
  `code_active` string, 
  `addon_restricted` string, 
  `addon_order_basis` string, 
  `addon_inv_item_type` string, 
  `include_in_order` string, 
  `initial_status` string, 
  `linked_club_type` string, 
  `linked_club_level` string, 
  `coupon_class` string, 
  `manual_price` string, 
  `use_as_client_prefer` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_am_code'