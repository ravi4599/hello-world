CREATE EXTERNAL TABLE `shipdw.hvtb_lnd_vxp_dxp_guestcheckindetail`(
  `guestcheckindetailid` string, 
  `boardinggroupnumber` int, 
  `pregnancyweeks` int, 
  `guestidentificationid` string, 
  `isidverified` boolean, 
  `isdocumentsretained` boolean, 
  `isphotoverified` boolean, 
  `iscashagreementgiven` boolean, 
  `iscruisecontractsigned` boolean, 
  `ismanualdocumentsverified` boolean, 
  `guestdigitalsignaturemediaitemid` string, 
  `addeddate` timestamp, 
  `addedby` string, 
  `lastmodifieddate` timestamp, 
  `lastmodifiedby` string, 
  `isguestbookletneeded` boolean, 
  `islegalguardiantravelling` boolean, 
  `cruisecontractsignedbyreservationguestid` boolean, 
  `ischildregistrationoptout` boolean, 
  `ispaymentpolicyaccepted` boolean, 
  `iscruisecontractsignedatpartylevel` boolean, 
  `selectedsecurityphotomediaitemid` string, 
  `selectedprofilephotomediaitemid` string, 
  `additionalidentificationdocumenttypecode` string, 
  `signedcruisecontractversion` string, 
  `isdocumentscollected` boolean, 
  `additionalidentificationid` string, 
  `loggedinuserid` string, 
  `loggedinusername` string, 
  `loggedinusermodifieddate` timestamp, 
  `isusresidence` boolean, 
  `isoptedforesta` boolean, 
  `documenttypecode` string, 
  `isitineraryrulesapplied` boolean, 
  `pregnancyweeksaddeddate` date, 
  `cruisecontractsigneddate` timestamp, 
  `signedhealthcontractversion` string, 
  `healthcontractsignedbyreservationguestid` string, 
  `healthcontractsigneddate` timestamp, 
  `cruisecontractsignedid` string, 
  `isfittotravel` boolean, 
  `isdocumentphotocopied` boolean, 
  `cruisecontractsignedbyemailid` string, 
  `isminorauthformcollected` boolean, 
  `iscruisewellcontractsigned` boolean, 
  `cruisewellcontractsignedby` string, 
  `signedcruisewellcontractversion` string, 
  `cruisewellcontractsigneddate` timestamp, 
  `cruisewellcontractsignedid` string, 
  `cruisewellcontractsignedbyemail` string, 
  `isthermalcheckpassed` boolean, 
  `thermalcheckdate` timestamp, 
  `iscovidtestpassed` boolean, 
  `covidtestdate` timestamp, 
  `healthcontractsignedby` string, 
  `ishealthpaperformcollected` boolean, 
  `iscruisecontractformcollected` boolean, 
  `healthsignaturemediaitemid` string, 
  `voyageid` string, 
  `batchtime` string, 
  `shipcode` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/landing/VXP/hvtb_lnd_vxp_guestcheckindetail';
  
  
  