CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_air_actual_leg`(
  `leg_id` int, 
  `header_id` int, 
  `segment_id` int, 
  `segment_seqn` int, 
  `leg_seqn` int, 
  `recloc` string, 
  `airline` string, 
  `operated_by` string, 
  `flight_no` string, 
  `departure_time` timestamp, 
  `departure_airport` string, 
  `arrival_time` timestamp, 
  `arrival_airport` string, 
  `air_cabin_class` string, 
  `airline_clos` string, 
  `n_of_stops` int, 
  `n_of_seats` int, 
  `flight_meal_code` string, 
  `duration_minutes` int, 
  `leg_seqn_ext` int, 
  `aircraft_code` string, 
  `codeshare_info` string, 
  `cng_airport_code` string, 
  `cng_aircraft_code` string, 
  `baggage_infolink` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_air_actual_leg'