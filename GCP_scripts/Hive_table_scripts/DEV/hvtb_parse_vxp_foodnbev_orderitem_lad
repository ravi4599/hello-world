CREATE EXTERNAL TABLE `shipdw.hvtb_parse_vxp_foodnbev_order_lad`(
  `orderid` string, 
  `venueid` string, 
  `locationid` string, 
  `orderdate` timestamp, 
  `guestid` string, 
  `status` int, 
  `tokennumber` int, 
  `colorcode` string, 
  `deliverypersonid` string, 
  `orderdeliverydate` timestamp, 
  `discount` decimal(18,2), 
  `taxamount` decimal(18,2), 
  `taxpercentage` decimal(5,2), 
  `gratuity` decimal(18,2), 
  `gratuitypercentage` decimal(5,2), 
  `amount` decimal(18,2), 
  `currency` string, 
  `paymentid` string, 
  `refundid` string, 
  `modifieddate` timestamp, 
  `ordernumber` int, 
  `delivertoguestid` string, 
  `servicechargepercentage` decimal(5,2), 
  `addedby` string, 
  `addeddate` timestamp, 
  `additionaldetails` string, 
  `deliveredatlocationcode` string, 
  `ordertypeid` string, 
  `orderoriginid` string, 
  `preorderslotsettingid` string, 
  `waittimeid` string, 
  `ispickupplaterequested` boolean, 
  `modifiedby` string, 
  `minwaittime` int, 
  `maxwaittime` int, 
  `hasotherallergy` boolean, 
  `locationtype` string, 
  `islocked` boolean, 
  `batchtime` timestamp, 
  `voyageid` string, 
  `shipcode` string, 
  `part_date` date)
STORED AS PARQUET LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/hvtb_parse_vxp_foodnbev_order_lad';
