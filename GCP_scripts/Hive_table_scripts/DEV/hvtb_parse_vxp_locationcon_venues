CREATE EXTERNAL TABLE shipdw.hvtb_parse_vxp_locationcon_venues(id bigint, name string, x bigint, y bigint, len bigint, width bigint, teleport_x bigint, teleport_y bigint, sector_id bigint, yaw_value bigint, capacity bigint, recommendation_venue_id bigint, global_venue_id string, crew_sailor_rate bigint, shipping_venue_id bigint, parent_id bigint, crew_only boolean, server_venue_id bigint,VoyageId STRING,BatchTime TIMESTAMP) partitioned by (Part_Date date) 
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
  LOCATION 'gs://vv-dev-nbx-cluster/data/parse/vxp/hvtb_parse_vxp_locationcon_venues';
  