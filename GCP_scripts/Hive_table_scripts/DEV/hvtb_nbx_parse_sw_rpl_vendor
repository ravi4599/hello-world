CREATE EXTERNAL TABLE `vv_db.hvtb_nbx_parse_sw_rpl_vendor`(
  `vendor_id` int, 
  `vendor_code` string, 
  `vendor_name_typed` string, 
  `vendor_name` string, 
  `contact` string, 
  `comments` string, 
  `is_active` string, 
  `email` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_nbx_parse_sw_rpl_vendor'