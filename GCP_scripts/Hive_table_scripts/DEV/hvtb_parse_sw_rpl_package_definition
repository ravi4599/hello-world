CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_package_definition`(
  `package_id` int, 
  `package_code` string, 
  `vacation_date` timestamp, 
  `package_type` string, 
  `sail_id` int, 
  `season_code` string, 
  `geog_area_code` string, 
  `is_active` string, 
  `comments` string, 
  `sort_seqn` int, 
  `shorex_mode` string, 
  `shorex_timing` string, 
  `initial_status` string, 
  `route_code` string, 
  `package_name` string, 
  `sail_segments` string, 
  `sail_days_override` int, 
  `points_sail_segments` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_package_definition';