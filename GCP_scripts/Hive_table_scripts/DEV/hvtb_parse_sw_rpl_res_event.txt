CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_event`(
  `record_id` int, 
  `res_event_type` string, 
  `res_id` int, 
  `event_timestamp` timestamp, 
  `operator_id` string, 
  `old_status` string, 
  `new_status` string, 
  `old_ship_code` string, 
  `old_sail_from` timestamp, 
  `old_sail_to` timestamp, 
  `new_ship_code` string, 
  `new_sail_from` timestamp, 
  `new_sail_to` timestamp, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_event'
