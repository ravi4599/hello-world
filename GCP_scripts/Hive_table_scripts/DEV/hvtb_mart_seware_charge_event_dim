drop table vv_db.hvtb_mart_seware_charge_event_dim;
CREATE EXTERNAL TABLE vv_db.hvtb_mart_seware_charge_event_dim(
  `charge_detail_id` BIGINT, 
  `src_record_id` BIGINT, 
  `rec_start_dttm` timestamp, 
  `rec_end_dttm` timestamp,
  `res_id` BIGINT, 
  `action_type` string, 
  `charge_id` BIGINT, 
  `charge_component_code` string, 
  `charge_state` string, 
  `guest_seqn` BIGINT, 
  `amount` double, 
  `amount_in_hdr_curr` double, 
  `explanation` string, 
  `change_area` string, 
  `src_package_id` BIGINT, 
  `action_timestamp` timestamp, 
  `is_added_manually` string, 
  `is_paid` string,
   paid_timestamp timestamp,   
  `comments` string, 
   load_dt timestamp,
   upd_dt timestamp,
   primaryhash string,
   md5_hash string
   )
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/seaware/hvtb_mart_seware_charge_event_dim';
 
