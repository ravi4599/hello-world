CREATE EXTERNAL TABLE shipdw.hvtb_mart_dim_dining_session(
voyage_id string, 
  dining_session_skey bigint, 
  dining_session_id string, 
  dining_session_name string, 
  session_day_name string, 
  session_voyage_number string, 
  venue_id string, 
  timeslot_duration_seconds int, 
  max_party_size int, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string) STORED AS PARQUET LOCATION 'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_dim_dining_session';