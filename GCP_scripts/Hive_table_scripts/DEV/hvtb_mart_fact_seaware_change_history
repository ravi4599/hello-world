CREATE EXTERNAL TABLE `vv_db.hvtb_mart_fact_seaware_change_history`(
  `change_id` bigint, 
  `src_res_id` bigint, 
  `attrib_name` string, 
  `paramtype` string, 
  `old_value` string, 
  `new_value` string, 
  `changed_by` string, 
  `changed_timestamp` timestamp, 
  `load_dt` timestamp, 
  `upd_dt` timestamp,)
PARTITIONED BY (part_dt date) STORED AS PARQUET LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_seaware_change_history'
;
gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_seaware_change_history

