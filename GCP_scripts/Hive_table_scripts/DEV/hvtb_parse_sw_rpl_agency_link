CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_agency_link`(
  `link_id` int, 
  `sub_agency_id` int, 
  `up_agency_id` int, 
  `link_type` string, 
  `date_from` timestamp, 
  `date_to` timestamp, 
  `comments` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_agency_link'
