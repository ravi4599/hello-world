  CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_sail_activity`(
  `sail_activity_id` int, 
  `sail_event_date_time` timestamp, 
  `sail_event_type` string, 
  `port_code` string, 
  `pier_code` string, 
  `may_embark` string, 
  `may_disembark` string, 
  `ship_code` string, 
  `relative_day` int, 
  `activity` string, 
  `comments` string, 
  `dep_arr_code` string, 
  `dep_arr_ref_id` int, 
  `es` string, 
  `ownership_code` string, 
  `setup_session_id` int, 
  `del_session_id` int, 
  `setup_from` timestamp, 
  `setup_to` timestamp, 
  `tender_mode` string, 
  `sail_event_ts` string, 
  `dress_code` string, 
  `is_active` string, 
  `sail_event_date_time_utc` timestamp, 
  `deck_only` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_sail_activity'
  
