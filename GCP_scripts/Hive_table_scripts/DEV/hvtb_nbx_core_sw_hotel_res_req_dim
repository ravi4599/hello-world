CREATE EXTERNAL TABLE `vv_db.hvtb_nbx_core_sw_hotel_res_req_dim`(
  `hotel_res_req_id` bigint, 
  `src_hotel_res_req_id` int, 
  `src_res_id` int, 
  `src_guest_id` int, 
  `request_type` string, 
  `start_date` timestamp, 
  `end_date` timestamp, 
  `city_code` string, 
  `hotel_id` int, 
  `hotel_category` string, 
  `room_seq_number` int, 
  `room_category` string, 
  `room_type` string, 
  `occupancy` int, 
  `hotel_space_type` string, 
  `allocation_id` int, 
  `inv_result` string, 
  `is_land_component` string, 
  `hotel_bed_type` string, 
  `effective_date` timestamp, 
  `etl_ld_status` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string) STORED AS PARQUET LOCATION 'gs://vv-dev-nbx-cluster/data/mart/seaware/hvtb_nbx_core_sw_hotel_res_req_dim';