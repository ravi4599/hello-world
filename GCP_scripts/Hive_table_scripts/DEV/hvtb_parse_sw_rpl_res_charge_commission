CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_res_charge_commission`(
`RECORD_ID` int,
`ENTITY_TYPE` string,
`ENTITY_ID` int,
`CHARGE_ID` int,
`COMMISS_CODE` string,
`AMOUNT` decimal(15,2),
`COMMISS_FARE` decimal(10,2),
`COMMISS_SOURCE` string,
`PERCENT` decimal(10,2),
`IS_MANUAL_ADJUSTMENT` string,
`AMOUNT_IN_HDR_CURR` decimal(15,2),
`AGENCY_ID` int,
`PAYOUT_DATE` timestamp,
`GL_PROTOTYPE_ID` int,
`op` string, 
`ts_ms` timestamp, 
`voyageid` string, 
`batchtime` timestamp)
PARTITIONED BY ( 
 `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_res_charge_commission';