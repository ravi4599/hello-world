CREATE EXTERNAL TABLE `shipdw.hvtb_mart_fact_virtual_queue`(
  `voyage_id` string, 
  `ship_code` string, 
  `voyage_skey` bigint, 
  `virtual_queue_detail_skey` bigint, 
  `virtual_queue_definition_skey` bigint, 
  `person_skey` bigint, 
  `queue_joined_datetime` timestamp, 
  `queue_canceled_datetime` timestamp, 
  `queue_notified_datetime` timestamp, 
  `queue_completed_datetime` timestamp, 
  `queue_total_wait_time_seconds` int, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
PARTITIONED BY ( 
  `part_dt` date)
STORED AS PARQUET
LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_virtual_queue';