CREATE EXTERNAL TABLE shipdw.hvtb_mart_fact_dining_reservation_crew(
voyage_id string,
voyage_skey bigint,
dining_reservation_detail_skey bigint,
served_by_skey bigint,
load_dt timestamp,
upd_dt timestamp,
primaryhash string, 
md5_hash string)

PARTITIONED BY (part_dt date) STORED AS PARQUET LOCATION
'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_dining_reservation_crew';