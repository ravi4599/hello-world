select count(*) from vv_db.hvtb_parse_sw_rpl_sail_header;

CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_sail_header`(
  `sail_id` int, 
  `ship_code` varchar(10), 
  `sail_date_from` timestamp, 
  `sail_date_to` timestamp, 
  `rel_day_from` int, 
  `rel_day_to` int, 
  `season_code` string, 
  `port_from` string, 
  `port_to` string, 
  `geog_area_code` string, 
  `is_fake` string, 
  `is_active` string, 
  `comments` string, 
  `sail_status` string, 
  `sail_code` string, 
  `dep_ref_id` int, 
  `arr_ref_id` int, 
  `route_code` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_sail_header';

