CREATE EXTERNAL TABLE `vv_db.hvtb_mart_lkp_sw_transfer_request`(
  `transfer_request_poi_id` bigint, 
  `point_of_interest` string, 
  `poi_address` string, 
  `poi_city` string, 
  `poi_zip` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_lkp_sw_transfer_request';
  
  
  
  
  
  
