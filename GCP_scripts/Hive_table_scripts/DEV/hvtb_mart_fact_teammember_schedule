drop table shipdw.hvtb_mart_fact_teammember_schedule;
CREATE EXTERNAL TABLE shipdw.hvtb_mart_fact_teammember_schedule(
  voyage_id string, 
  ship_code string, 
  voyage_skey bigint, 
  person_skey bigint, 
  venue_skey bigint, 
  schedule_date_skey bigint, 
  schedule_date date, 
  schedule_day_period string, 
  team_member_role string, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
PARTITIONED BY (part_dt date) STORED AS PARQUET LOCATION
  'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_teammember_schedule'; 

