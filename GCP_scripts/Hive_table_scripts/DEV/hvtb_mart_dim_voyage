



CREATE EXTERNAL TABLE shipdw.hvtb_mart_dim_voyage(voyage_skey BIGINT, voyage_id STRING, voyage_number STRING, voyage_guid STRING, cruise_id BIGINT, voyage_description STRING, voyage_start_date TIMESTAMP, voyage_end_date TIMESTAMP, voyage_length INT, voyage_status STRING, ship_guid STRING, ship_code STRING, ship_name STRING, isseaday INT, actual_capacity_days INT, booked_capacity_days INT, actual_sailor_days INT, built_capacity_days INT, sailed_cabin_occupancy_pct INT, active_status STRING, src_deleted_flag BOOLEAN, src_active_flag BOOLEAN, next_voyage_number STRING, previous_voyage_number STRING, next_voyage_skey BIGINT, previous_voyage_skey BIGINT, load_dt TIMESTAMP, upd_dt TIMESTAMP, primaryhash STRING, md5_hash STRING)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 'gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_dim_voyage'


