##RequestType##

CREATE EXTERNAL TABLE `shipdw`.`hvtb_parse_vxp_housekeeping_request_types`(`name` string, `id` int, `at_risk_duration` bigint, `category_id` bigint, `is_cleaning` boolean, `priority` int, `request_duration` bigint, `sla_duration` bigint, `voyageid` string, `batchtime` timestamp, `shipcode` string)
PARTITIONED BY (`part_date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.serde2.lazy.LazySimpleSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.mapred.TextInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.HiveIgnoreKeyTextOutputFormat'
LOCATION 'gs://vv-dev-nbx-cluster/data/parse/vxp/hvtb_parse_vxp_housekeeping_request_types';
