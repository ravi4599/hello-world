CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_group_booking`(
  `group_id` int, 
  `comments` string, 
  `agency_id` int, 
  `agent_id` int, 
  `group_name` string, 
  `group_type` string, 
  `n_of_guests` int, 
  `rel_day_from` int, 
  `rel_day_to` int, 
  `sail_date_from` timestamp, 
  `sail_date_to` timestamp, 
  `ship_code` string, 
  `is_active` string, 
  `is_valid` string, 
  `probability` decimal(7,4), 
  `group_status` string, 
  `allow_diff_agency` string, 
  `expiration_extension` int, 
  `group_init_date` timestamp, 
  `wdw_group_id` string, 
  `priority` int, 
  `termination_completed` string, 
  `group_size_override` int, 
  `probability_allocated` decimal(7,4), 
  `is_contract_rate` string, 
  `can_process_gtb` string, 
  `grp_coord_name` string, 
  `use_tc_fullpay` string, 
  `currency_code` string, 
  `sec_agency_id` int, 
  `language_code` string, 
  `is_invoice_valid` string, 
  `is_charter` string, 
  `office_code` string, 
  `is_prob_manual` string, 
  `allow_unbalance` string, 
  `currency_rate` decimal(10,5), 
  `is_unbalanced` string, 
  `gb_entity_type` string, 
  `invoice_version` int, 
  `first_full_payment_processed` timestamp, 
  `allow_auto_process_fpr` string, 
  `default_insurance` string, 
  `am_order_id` int, 
  `fpr_in_process` string, 
  `company_id` int, 
  `update_version` int, 
  `dep_ref_id` int, 
  `arr_ref_id` int, 
  `is_paysch_manual` string, 
  `sec_agent_id` int, 
  `company_agent_id` int, 
  `client_id` int, 
  `vt_package_id` int, 
  `ownership_code` string, 
  `invoice_gen_deferred` string, 
  `vac_date_from` timestamp, 
  `vac_date_to` timestamp, 
  `terms_confirmed` string, 
  `source_code` string, 
  `group_mode` string, 
  `allow_invoicing` string, 
  `inventory_release_date` timestamp, 
  `use_fit_paysch` string, 
  `tc_sail_id` int, 
  `extra_agency_id` int, 
  `process_as_whole_entity` string, 
  `auto_distribute_money` string, 
  `tour_package_id` int, 
  `allow_diff_curr_rate` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
   'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_group_booking'
  