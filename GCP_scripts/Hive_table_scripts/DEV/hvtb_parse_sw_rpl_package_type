CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_package_type`(
  `package_type_id` int, 
  `package_type` string, 
  `land_days` int, 
  `sail_days` int, 
  `comments` string, 
  `product_type` string, 
  `tc_package_type` string, 
  `land_days_post` int, 
  `is_land_only` string, 
  `is_multi_sail` string, 
  `is_shorex` string, 
  `is_secondary` string, 
  `is_active` string, 
  `shorex_timing` string, 
  `extra_seat_question` string, 
  `pre_post_mode` string, 
  `package_class` string, 
  `sail_segments` string, 
  `capacity` int, 
  `allow_segments` string, 
  `package_type_name` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `shipcode` string, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_package_type';