CREATE EXTERNAL TABLE `vv_db.hvtb_parse_sw_rpl_ship_dining_request`(
  `request_id` int, 
  `owner_type` string, 
  `owner_id` int, 
  `facility_id` int, 
  `dining_date` timestamp, 
  `dining_kind` string, 
  `dining_time_from` timestamp, 
  `dining_time_to` timestamp, 
  `dining_duration` int, 
  `party_size` int, 
  `attachment_id` int, 
  `inventory_request_type` string, 
  `allocation_id` int, 
  `comments` string, 
  `external_guest_count` int, 
  `effective_date` timestamp, 
  `notes` string, 
  `assign_mode` string, 
  `responsible_guest` int, 
  `requested_start_time` timestamp, 
  `quantity` int, 
  `ownership_mode` string, 
  `sitting_code` string, 
  `allow_multiple_tables` string, 
  `no_show_count` int, 
  `is_sail_canceled` string, 
  `parent_addon_code` string, 
  `secret_code` string, 
  `secret_code_valid` string, 
  `op` string, 
  `ts_ms` timestamp, 
  `voyageid` string, 
  `batchtime` timestamp)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/parse/seaware/hvtb_parse_sw_rpl_ship_dining_request';