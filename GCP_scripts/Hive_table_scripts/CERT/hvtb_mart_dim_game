CREATE EXTERNAL TABLE shipdw.hvtb_mart_dim_game(
  voyage_id string, 
  game_skey bigint, 
  game_id string, 
  game_name string, 
  game_master_prom string, 
  game_is_bet_type_flag boolean, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
  STORED AS PARQUET LOCATION 'gs://vv-cert-nbx-cluster/data/mart/hvtb_mart_dim_game';