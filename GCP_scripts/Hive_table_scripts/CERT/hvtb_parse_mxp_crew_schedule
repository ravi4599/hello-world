CREATE EXTERNAL TABLE `shipdw.hvtb_parse_mxp_crew_schedule`(
  `id` string, 
  `externalid` bigint, 
  `person_internalcode` bigint, 
  `person_globalcode` bigint, 
  `person_guid` string, 
  `person_firstname` string, 
  `person_lastname` string, 
  `person_company` string, 
  `manningagent` string, 
  `installation` string, 
  `activitytype` string, 
  `clockin_time` timestamp, 
  `clockin_clock` string, 
  `clockin_manualentry` boolean, 
  `clockout_time` timestamp, 
  `clockout_clock` string, 
  `clockout_manualentry` boolean, 
  `confirmed` boolean, 
  `created` timestamp, 
  `createdby` string, 
  `updated` timestamp, 
  `updatedby` string, 
  `isdeleted` boolean, 
  `isapproved` boolean, 
  `ts` string, 
  `cn` string, 
  `n` string, 
  `ver` bigint, 
  `plt` string, 
  `mrc` bigint, 
  `rc` bigint, 
  `ri` bigint, 
  `voyageid` string, 
  `batchtime` timestamp, 
  `shipcode` string)
PARTITIONED BY ( 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-cert-nbx-cluster/data/parse/mxp/hvtb_parse_mxp_crew_schedule'