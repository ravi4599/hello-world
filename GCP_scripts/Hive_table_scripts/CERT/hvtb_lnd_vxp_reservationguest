CREATE EXTERNAL TABLE shipdw.hvtb_lnd_vxp_reservationguest(
  reservationguestid string, 
  reservationid string, 
  guestid string, 
  guesttypeid string, 
  isvip boolean, 
  isprimary boolean, 
  embarkdate date, 
  debarkdate date, 
  stateroom string, 
  reservationstatusid string, 
  guestaddressid string, 
  berthnumber string, 
  loyaltyleveltypecode string, 
  sequencenumber int, 
  price decimal(9,2), 
  insuranceamount decimal(9,2), 
  taxamount decimal(9,2), 
  insurancecode string, 
  isinsurancedeclined boolean, 
  addeddate timestamp, 
  addedby string, 
  lastmodifieddate timestamp, 
  lastmodifiedby string, 
  diningrotation string, 
  diningtable string, 
  diningroom string, 
  diningtime string, 
  isroutedto boolean, 
  selectedaddressid string, 
  guestemergencycontactid string, 
  guestcheckindetailid string, 
  gratuityamount decimal(9,2), 
  guesttypecode string, 
  reservationstatuscode string, 
  isbacktoback boolean, 
  pmsid string, 
  embarkportcode string, 
  debarkportcode string, 
  cbpstatus string, 
  cbpstatuschangeddate timestamp, 
  iswearableactivated boolean, 
  pin string, 
  isassociatedflightbooking boolean, 
  voyageid string, 
  batchtime string, 
  shipcode string)
PARTITIONED BY ( 
  part_date date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-cert-nbx-cluster/data/landing/vxp/hvtb_lnd_vxp_reservationguest';