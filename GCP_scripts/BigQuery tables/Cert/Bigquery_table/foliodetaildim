CREATE EXTERNAL TABLE hive_schema_stg.foliodetaildim(voyage_id string,
 folio_detail_skey bigint, 
 folio_detail_id bigint,
  currency_code string,
   location string, 
   line_type string, 
   line_status string,
    line_description string,
     sale_comment string, 
     sale_discount_comment string, 
     src_deleted_flag boolean, 
     load_dt timestamp,
      upd_dt timestamp, 
      primaryhash string, 
      md5_hash string)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-cert-nbx-cluster/data/mart/foliodetaildim/*']
);