CREATE EXTERNAL TABLE hive_schema_stg.voyage_well_questionaire_dim(voyage_well_question_skey BIGINT, voyage_id STRING, question_type STRING, question_code STRING, question_title STRING, question_resource STRING, src_deleted_flg BOOLEAN, load_dt TIMESTAMP, upd_dt TIMESTAMP, primaryhash STRING, md5_hash STRING)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-cert-nbx-cluster/data/mart/hvtb_mart_dim_voyage_well_questionaire/*']
);


CREATE OR REPLACE VIEW shipdw.voyage_well_questionaire_dim AS SELECT * FROM hive_schema_stg.voyage_well_questionaire_dim;