CREATE EXTERNAL TABLE hive_schema_stg.specialrequest_dim(voyage_id STRING, specialrequest_skey BIGINT, specialrequest_id BIGINT, person_id INT, specialrequest_guid STRING, specialrequest_type STRING, specialrequest_type_code STRING, specialrequest_date TIMESTAMP, specialrequest_description STRING, specialrequest_comment STRING, specialrequest_upd_date TIMESTAMP, src_deleted_flag BOOLEAN, load_dt TIMESTAMP, upd_dt TIMESTAMP, primaryhash STRING, md5_hash STRING)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-cert-nbx-cluster/data/mart/hvtb_mart_dim_specialrequest/*']
);
CREATE OR REPLACE VIEW shipdw.specialrequest_dim AS SELECT * FROM hive_schema_stg.specialrequest_dim;
