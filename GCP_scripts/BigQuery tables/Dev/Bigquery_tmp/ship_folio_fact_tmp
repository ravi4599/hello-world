CREATE EXTERNAL TABLE hive_schema_stg.ship_folio_fact_tmp(
	voyage_id string,
	folio_detail_skey bigint,
	folio_item_skey bigint,
	person_skey bigint,
	ship_code string,
	voyage_skey bigint,
	itinerary_skey bigint,
	sale_date_skey bigint,
	sale_time_skey bigint,
	booking_reference_number string,
	sale_quantity int,
	debit_amount float,
	credit_amount float,
	discount_amount float,
	total_collected_sales_amount float,
	load_dt timestamp,
	upd_dt timestamp,
	primaryhash string,  
	md5_hash string,
	part_dt date)
	OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_ship_folio/*']
);