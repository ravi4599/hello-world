CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.sw_forecastversion_lkp_tmp(
 `forecast_version_id` bigint, 
  `forecast_version_desc` string, 
  `etl_ld_status` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp,
  `primaryhash` string,
  `md5_hash` string)
OPTIONS (
format = 'PARQUET',
uris = ['gs://vv-dev-nbx-cluster/data/temp/hvtb_nbx_core_sw_forecastversion_lkp/*']
);