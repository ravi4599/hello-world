CREATE EXTERNAL TABLE `hive_schema_stg.postvoyage_survey_question_answer_dim_tmp`(
  `question_answer_id` bigint, 
  `src_question_id` string, 
  `variable_id` string, 
  `question_text` string, 
  `type` string, 
  `category` string, 
  `answer_code` int, 
  `answer_code_text` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/core/seaware/temp/hvtb_mart_postvoyage_survey_question_answer_dim/*']
);
