CREATE EXTERNAL TABLE `hive_schema_stg.SEWARE_TRANSFER_REQUEST_POI_LKP_tmp`(
  `transfer_request_poi_id` bigint, 
  `point_of_interest` string, 
  `poi_address` string, 
  `poi_city` string, 
  `poi_zip` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/temp/hvtb_mart_lkp_sw_transfer_request/*']
);
       
