CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.sw_plan_forecast_header_dim_tmp(
  plan_forecast_id bigint,
rec_start_dttm timestamp,
rec_end_dttm timestamp,
src_header_id int,
sail_id int,
package_type string,
cabin_ctg_generic string,
forecast_version_id int,
forecast_metric_id int,
comments string,
load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
OPTIONS (
format = 'PARQUET',
uris = ['gs://vv-dev-nbx-cluster/data/temp/hvtb_nbx_core_sw_plan_forecast_header_dim/*']
);