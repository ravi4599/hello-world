CREATE EXTERNAL TABLE `hive_schema_stg.SEWARE_TRANSFER_REQUEST_LEG_DETAILS_REL_tmp`(
  `transfer_request_leg_detail_id` bigint, 
  `rec_start_dttm` timestamp, 
  `rec_end_dttm` timestamp, 
  `src_res_id` int, 
  `src_sail_id` int, 
  `pickup_date` timestamp, 
  `primary_sailor_name` string, 
  `number_of_passengers` int, 
  `qty_of_luggage` int, 
  `pickup_time` timestamp, 
  `pickup_poi` int, 
  `pickup_location` string, 
  `pickup_address` string, 
  `pickup_city` string, 
  `pickup_zip` string, 
  `dropoff_poi` int, 
  `dropoff_location` string, 
  `dropoff_address` string, 
  `dropoff_city` string, 
  `dropoff_zip` string, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/temp/hvtb_mart_sw_transreq_legdetails_rel/*']
);
       

