CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.voyage_well_questionaire_fact_err
(
  voyage_id string, 
  ship_code string, 
  voyage_skey bigint, 
  person_skey bigint, 
  voyage_well_question_skey bigint, 
  voyage_well_answer string, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string,
  part_dt date
)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_voyage_well_questionaire_err/*']
 );