CREATE  EXTERNAL TABLE hive_schema_stg.person_position_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_person_position/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_person_position/'
);

CREATE OR REPLACE VIEW shipdw.person_position_fact as SELECT * FROM hive_schema_stg.person_position_fact;