CREATE  EXTERNAL TABLE hive_schema_stg.person_status_history_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_person_status_history_fact/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_person_status_history_fact/'
);

CREATE OR REPLACE VIEW shipdw.person_status_history_fact AS SELECT * FROM hive_schema_stg.person_status_history_fact;