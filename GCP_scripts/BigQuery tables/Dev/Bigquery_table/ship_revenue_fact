
  

 
 
 
 CREATE  EXTERNAL TABLE hive_schema_stg.ship_revenue_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_revenue/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_revenue/'
);

CREATE OR REPLACE VIEW shipdw.ship_revenue_fact as SELECT * FROM hive_schema_stg.ship_revenue_fact;


