CREATE or replace EXTERNAL TABLE hive_schema_stg.SEAWARE_PROMOTION_DIM        
(
 res_promo_id integer,
 res_id integer, 
  guest_id integer, 
  package_id integer, 
  addon_id integer, 
  carddeck_id integer, 
  shorex_id integer, 
  hotel_id integer, 
  promotion_id integer, 
  rec_start_dttm timestamp, 
  rec_end_dttm timestamp, 
  is_active string, 
  is_excluded string, 
  promo_mode string,  
  etl_ld_status string, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/mart/seaware/hvtb_nbx_core_sw_promotion_dim/*']
); 

create view seaware..SEAWARE_PROMOTION_DIM as select * from hive_schema_stg.SEAWARE_PROMOTION_DIM  
