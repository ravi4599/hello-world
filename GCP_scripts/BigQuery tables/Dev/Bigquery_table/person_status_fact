CREATE  EXTERNAL TABLE hive_schema_stg.person_status_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_person_status/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_person_status/'
);

CREATE OR REPLACE VIEW shipdw.person_status_fact as SELECT * FROM hive_schema_stg.person_status_fact;