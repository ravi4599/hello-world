CREATE  EXTERNAL TABLE hive_schema_stg.crew_activity_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_crew_activity/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_crew_activity/'
);


create or replace view shipdw.crew_activity_fact as select * from hive_schema_stg.crew_activity_fact;