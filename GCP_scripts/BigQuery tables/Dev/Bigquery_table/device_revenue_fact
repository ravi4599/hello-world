



CREATE  EXTERNAL TABLE hive_schema_stg.device_revenue_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_device_revenue/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_device_revenue/'
);


create or replace view shipdw.device_revenue_fact as select * from hive_schema_stg.device_revenue_fact;


