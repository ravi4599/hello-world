CREATE EXTERNAL TABLE hive_schema_stg.seware_hotel_lkp(
  `hotel_id` int64,
   REC_START_DTTM timestamp,
  REC_END_DTTM timestamp,
  `hotel_code` string, 
  `hotel_name` string, 
  `hotel_type` string, 
  `is_active` string, 
  `hotel_rating` int, 
  `address_type` string, 
  `address_line1` string, 
  `address_line2` string, 
  `address_line3` string, 
  `address_line4` string, 
  `address_city` string, 
  `state_code` string, 
  `zip` string, 
  `country_code` string, 
   load_dt timestamp,
   upd_dt timestamp,
   primaryhash string,
   md5_hash string
   )
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_seware_hotel_lkp/*']);

