CREATE  EXTERNAL TABLE hive_schema_stg.dining_session_fact
WITH PARTITION COLUMNS
OPTIONS (
  uris=['gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_dining_session/*'],
  format=PARQUET,
  hive_partition_uri_prefix='gs://vv-dev-nbx-cluster/data/mart/hvtb_mart_fact_dining_session/'
  );
  
  CREATE OR REPLACE VIEW shipdw.dining_session_fact AS SELECT * FROM hive_schema_stg.dining_session_fact;