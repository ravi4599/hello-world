CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.seaware_flight_dim(
flight_id bigint,
flight_no string,
rec_start_dttm timestamp,
rec_end_dttm timestamp,
airline string,
departure_airport_name string,
origin string,
departure_date date,
departure_time timestamp,
arrival_airport_name string,
destination string,
arrival_date date,
arrival_time timestamp,
operated_by string,
airline_rloc string,
n_of_stops int,
load_dt timestamp, 
upd_dt timestamp, 
primaryhash string, 
md5_hash string)
OPTIONS (
format = 'PARQUET',
uris = ['gs://vv-dev-nbx-cluster/data/mart/seaware/hvtb_nbx_core_sw_flight_dim/*']
);
  
CREATE OR REPLACE VIEW seaware.seaware_flight_dim AS SELECT * FROM hive_schema_stg.seaware_flight_dim;