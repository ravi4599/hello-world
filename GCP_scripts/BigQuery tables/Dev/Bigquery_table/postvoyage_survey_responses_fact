CREATE EXTERNAL TABLE `hive_schema_stg.postvoyage_survey_responses_fact`(
  `request_id` varchar(100), 
  `client_id` bigint, 
  `ship_id` bigint, 
  `sail_id` bigint, 
  `guest_id` bigint, 
  `request_time` timestamp, 
  `response_time` timestamp, 
  `itinerary_port` varchar(50), 
  `activity_skey` bigint, 
  `question_id` bigint, 
  `answer_rating` decimal(6,3), 
  `answer_text` varchar(1000), 
  `question_answer_id` int, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string,
  `part_dt` date)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/core/seaware/hvtb_nbx_core_postvoyage_survey_responses_fact/*']
 );


CREATE OR REPLACE VIEW seaware.postvoyage_survey_responses_fact as SELECT * FROM hive_schema_stg.postvoyage_survey_responses_fact;

CREATE EXTERNAL TABLE `hive_schema_stg.postvoyage_survey_responses_fact_tmp`(
  `request_id` varchar(100), 
  `client_id` bigint, 
  `ship_id` bigint, 
  `sail_id` bigint, 
  `guest_id` bigint, 
  `request_time` timestamp, 
  `response_time` timestamp, 
  `itinerary_port` varchar(50), 
  `activity_skey` bigint, 
  `question_id` bigint, 
  `answer_rating` decimal(6,3), 
  `answer_text` varchar(1000), 
  `question_answer_id` int, 
  `load_dt` timestamp, 
  `upd_dt` timestamp, 
  `primaryhash` string, 
  `md5_hash` string,
  `part_dt` date)
OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/core/seaware/temp/hvtb_nbx_core_postvoyage_survey_responses_fact/*']
 );
