CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.seaware_promotion_lkp(
  promotion_id bigint, 
  promo_code string, 
  promo_group string, 
  promo_name string,
  etl_ld_status string,
  load_dt timestamp,
  upd_dt timestamp)
OPTIONS (
format = 'PARQUET',
uris = ['gs://vv-dev-nbx-cluster/data/mart/seaware/hvtb_nbx_core_sw_promotion_lkp/*']
);

CREATE OR REPLACE VIEW seaware.seaware_promotion_lkp AS SELECT * FROM hive_schema_stg.seaware_promotion_lkp;