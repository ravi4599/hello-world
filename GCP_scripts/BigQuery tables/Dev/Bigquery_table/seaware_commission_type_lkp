CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.seaware_commission_type_lkp(
  commission_type_id bigint, 
  commission_code string, 
  commission_source string, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash string, 
  md5_hash string)
OPTIONS (
format = 'PARQUET',
uris = ['gs://vv-dev-nbx-cluster/data/mart/hvtb_nbx_core_swrpl_commission_type_lkp/*']
);

CREATE OR REPLACE VIEW seaware.seaware_commission_type_lkp AS SELECT * FROM hive_schema_stg.seaware_commission_type_lkp;