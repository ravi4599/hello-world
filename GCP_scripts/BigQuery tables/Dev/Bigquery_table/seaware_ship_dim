CREATE OR REPLACE EXTERNAL TABLE hive_schema_stg.seaware_ship_dim(
ship_id bigint,
ship string ,
ship_name string,
load_dt timestamp, 
upd_dt timestamp, 
primaryhash string, 
md5_hash string)                     

OPTIONS (
  format = 'PARQUET',
  uris = ['gs://vv-dev-nbx-cluster/data/mart/hvtb_nbx_core_sw_ship_dim/*']
);



CREATE OR REPLACE VIEW seaware.seaware_ship_dim AS SELECT * FROM hive_schema_stg.seaware_ship_dim;