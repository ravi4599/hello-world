drop table vv_db.hvtb_lnd_sfdc_surveyresponses;

CREATE EXTERNAL TABLE `vv_db.hvtb_lnd_sfdc_surveyresponses`(
  `id` string, 
  `case_number__c` string, 
  `createddate` timestamp, 
  `createdbyid` string, 
  `comments__c` string, 
  `question__c` string, 
  `response__c` string, 
  `surveyquestion__c` string, 
  `survey_sent__c` timestamp, 
  `lastmodifieddate` timestamp, 
  `batchtime` timestamp, 
  `part_date` date)
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  'gs://vv-dev-nbx-cluster/data/landing/sfdc/hvtb_lnd_sfdc_surveyresponses';
  
   create or replace view vv_db.hvtb_parse_sfdc_surveyresponses as select * from vv_db.hvtb_lnd_sfdc_surveyresponses;