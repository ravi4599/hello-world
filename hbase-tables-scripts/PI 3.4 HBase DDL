CREATE TABLE HBTB_NBX_SAILOR_RESERVATION_ACTIVITY_BOOKING
(
   RESERVATIONID BIGINT not null,
   LOYALTYMEMBERSHIPID BIGINT not null,
   VOYAGEID VARCHAR,
   ACTIVITIES_ARRAYJSONRAW VARCHAR,
   ACTIVITY_JSONRAW VARCHAR,
   SELECTEDPACKAGE_ACTIVITYPACKAGECODE VARCHAR,
   SELECTEDPACKAGE_PACKAGETYPECODE BIGINT,
   SELECTEDPACKAGE_STATUS BIGINT,
   SELECTEDPACKAGE_CRUISEPACKAGECODE VARCHAR,
   SELECTEDPACKAGE_START VARCHAR,
   SELECTEDPACKAGE_DURATION VARCHAR,
   SELECTEDPACKAGE_END VARCHAR,
   ISACTIVE VARCHAR(20),
   LASTMODIFIED TIMESTAMP,
   CONSTRAINT PK PRIMARY KEY (RESERVATIONID,LOYALTYMEMBERSHIPID,VOYAGEID,SELECTEDPACKAGE_ACTIVITYPACKAGECODE)
);


ALTER TABLE HBTB_NBX_SEAWARE_RESERVATION add SELECTEDSAILINGGROUP_VOYAGEID varchar;


CREATE TABLE HBTB_NBX_CMS_ACTIVITY  
(
   UUID VARCHAR NOT NULL,
   NAME VARCHAR,
   PRIORITY VARCHAR,
   LD VARCHAR,
   SD VARCHAR,
   EXTERNALID VARCHAR,
   TAGS VARCHAR,
   TAGAPP_MOOD VARCHAR,
   TAGAPP_KEYWORDS VARCHAR,
   EVENTTYPE VARCHAR,
   ISACTIVE VARCHAR,
   MD5KEY VARCHAR,
   LASTMODIFIED TIMESTAMP
   CONSTRAINT PK PRIMARY KEY (UUID)
);


CREATE TABLE HBTB_NBX_SAILOR_CALENDAR_ACTIVITY_STREAM 
(
  ROWKEY VARCHAR not null,
  RESID BIGINT,
  SAILORID BIGINT,
  VOYAGEID VARCHAR,
  VOY_START VARCHAR,
  VOY_END VARCHAR,
  LASTMODIFIED TIMESTAMP
  CONSTRAINT PK PRIMARY KEY (ROWKEY)
);



CREATE TABLE HBTB_NBX_SAILOR_CALENDAR_ACTIVITY_STREAM_HISTORY 
(
  ROWKEY VARCHAR not null,
  RESID BIGINT,
  SAILORID BIGINT,
  VOYAGEID VARCHAR,
  VOY_START VARCHAR,
  VOY_END VARCHAR,
  LASTMODIFIED TIMESTAMP
  CONSTRAINT PK PRIMARY KEY (ROWKEY)
);


CREATE TABLE HBTB_NBX_SAILOR_CAL_FEEDBACK_STREAM
(
   SAILORID VARCHAR,
   ACTIVITYID VARCHAR,
   NBXUNIQUEKEY VARCHAR,
   REQUESTSOURCE VARCHAR,
   SAILORSELECTION VARCHAR,
   FEEDBACKLOOPCALLFLAG VARCHAR DEFAULT 'N',
   TIMESTAMP TIMESTAMP,
   ROWKEY VARCHAR PRIMARY KEY
);

CREATE TABLE HBTB_NBX_SAILOR_CAL_FEEDBACK_STREAM_HISTORY
(
   SAILORID VARCHAR,
   ACTIVITYID VARCHAR,
   NBXUNIQUEKEY VARCHAR,
   REQUESTSOURCE VARCHAR,
   SAILORSELECTION VARCHAR,
   FEEDBACKLOOPCALLFLAG VARCHAR DEFAULT 'N',
   TIMESTAMP TIMESTAMP,
   ROWKEY VARCHAR PRIMARY KEY
);


CREATE TABLE HBTB_NBX_PACKAGE_TYPE_ATTRIBUTES
(
   PACKAGE_TYPE VARCHAR NOT NULL,
   LVL_ACTIVITY VARCHAR,
   EXP_TYPE VARCHAR,
   MINIMUM_CAP BIGINT,
   PROFITABILITY VARCHAR,
   TRIBE VARCHAR,
   LASTMODIFIED TIMESTAMP,
   CONSTRAINT PK PRIMARY KEY (PACKAGE_TYPE)
);


CREATE TABLE HBTB_NBX_CALENDAR_RECOMMENDATION_SOURCE_REQUEST
(
   CORRELATIONID varchar PRIMARY KEY,
   REQUESTSOURCE varchar,
   CLIENTID varchar,
   GROUPCLIENTID varchar,
   LOCATIONS varchar,
   RESERVATIONID varchar,
   NUMBEROFRECOMMENDATIONS varchar,
   STARTDATETIMEMINRANGE varchar,
   STARTDATETIMEMAXRANGE varchar,
   TIMESTAMP timestamp
)
; 

CREATE TABLE HBTB_NBX_CALENDAR_RECOMMENDATION_SOURCE_RESPONSE
(
   CORRELATIONID varchar not null,
   NBXUNIQUEKEY varchar not null,
   REQUESTSOURCE varchar ,
   TIMESTAMP timestamp,
   CONSTRAINT PK PRIMARY KEY (CORRELATIONID,NBXUNIQUEKEY)
)
; 

CREATE TABLE HBTB_NBX_CALENDAR_RECOMMENDATIONS
(
   NBXUNIQUEKEY varchar PRIMARY KEY,
   SAILORID varchar,
   ACTIVITYID varchar,
   CATEGORY varchar,
   DESCRIPTION varchar,
   RANKING varchar,
   START_DATETIME varchar,
   END_DATETIME varchar,
   ISACTIVE varchar DEFAULT 'Y',
   ISRECOMMENDED varchar DEFAULT 'N',
   correlationid varchar,
   voyageid varchar,
   TIMESTAMP timestamp
);
