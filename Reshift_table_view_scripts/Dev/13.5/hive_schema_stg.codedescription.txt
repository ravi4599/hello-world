CREATE EXTERNAL TABLE hive_schema_stg.codedescription(
voyage_id varchar(100),
code_skey bigint,
code_type varchar(100),
activity_id varchar(100),
activity_slot_code varchar(100),
code varchar(100),
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(50),
md5_hash varchar(50))
STORED AS PARQUET
LOCATION
's3://vv-dev-emr-cluster/data/mart/codedescription'