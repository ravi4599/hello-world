
CREATE EXTERNAL TABLE hive_schema_stg.org_unit_dim (
	org_unit_skey int8 ,
	voyage_id varchar(50) ,
	org_unit_id int ,
	org_unit_name varchar(50) ,
	org_unit_abbreviation varchar(50) ,
	load_dt timestamp ,
	upd_dt timestamp ,
	primaryhash varchar(50),
	md5_hash varchar(50))STORED AS PARQUET
LOCATION
's3://vv-dev-emr-cluster/data/mart/hvtb_mart_dim_org_unit'

CREATE OR REPLACE VIEW shipdw.org_unit_dim AS SELECT * FROM hive_schema_stg.org_unit_dim with no schema binding;