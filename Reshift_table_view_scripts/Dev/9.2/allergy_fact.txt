CREATE EXTERNAL TABLE hive_schema_stg.allergy_fact(
voyage_id varchar(50), 
order_detail_skey int8, 
menu_item_skey int8, 
dining_reservation_detail_skey int8, 
person_skey int8, 
allergy_skey int8, 
allergy_details_category varchar(50), 
src_deleted_flg boolean, 
load_dt timestamp, 
upd_dt timestamp)
PARTITIONED BY ( 
part_dt date)
STORED AS PARQUET
LOCATION
's3://vv-dev-emr-cluster/data/mart/hvtb_mart_fact_allergy'

CREATE OR REPLACE VIEW shipdw.allergy_fact AS SELECT * FROM hive_schema_stg.allergy_fact with no schema binding;