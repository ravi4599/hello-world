drop table hive_schema_stg.activity_dim cascade

drop view shipdw.activity_dim


CREATE EXTERNAL TABLE hive_schema_stg.activity_dim(
activity_skey int8, 
voyage_id varchar(50), 
activity_id varchar(50),
activity_cms_id varchar(50),
activity_code varchar(50),
activity_type varchar(50),
activity_level varchar(50),
activity_name varchar(50),
activity_group_code varchar(50),
activity_port_code varchar(50),
activity_description varchar(50),
currency_code varchar(50),
multiple_booking_allowed_flg boolean, 
price_type varchar(50), 
vip_flg boolean,
giftable_flg boolean,
packageable_flg boolean,
bookable_flg boolean,
ada_accessible_flg boolean,
minimum_age int4,
maximum_age int4,
access_type varchar(50),
waitlist_enable_flg boolean,
booking_closed_duration int4,
activity_setup_duration int4,
activity_duration int4,
activity_location_type varchar(50),
enabled_flg boolean, 
src_deleted_flg boolean, 
load_dt timestamp, 
upd_dt timestamp, 
primaryhash varchar(50), 
md5_hash varchar(50))
STORED AS PARQUET
LOCATION
's3://vv-staging-emr-cluster/data/mart/hvtb_mart_dim_activity'

CREATE OR REPLACE VIEW shipdw.activity_dim AS SELECT * FROM hive_schema_stg.activity_dim with no schema binding;