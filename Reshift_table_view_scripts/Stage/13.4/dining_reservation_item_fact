CREATE EXTERNAL TABLE hive_schema_stg.dining_reservation_item_fact(
	voyage_id varchar(100),
	voyage_skey bigint,
	ship_code varchar(100),
	dining_reservation_detail_skey bigint,
	dining_menu_skey int,
	dining_menu_item_skey int,
	sale_detail_dim_skey bigint,
	pos_item_skey bigint,
	dining_item_ordered_datetime timestamp,
	dining_item_served_datetime timestamp,
	dining_item_fulfillment_time_in_seconds int,
	meal_period_name varchar(100),
	dining_guest_order_status varchar(100),
	load_dt timestamp,
	upd_dt timestamp,
	primaryhash varchar(50),
	md5_hash varchar(50),
	part_dt date)
	STORED AS PARQUET LOCATION 's3://vv-staging-emr-cluster/data/mart/hvtb_mart_fact_dining_reservation_item';
	
CREATE OR REPLACE VIEW shipdw.dining_reservation_item_fact AS SELECT * FROM hive_schema_stg.dining_reservation_item_fact with no schema binding;