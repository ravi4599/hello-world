DROP TABLE hive_schema_stg.device_revenue_fact;
CREATE EXTERNAL TABLE hive_schema_stg.device_revenue_fact (
voyage_id varchar(100),
ship_code varchar(100),
voyage_skey int8,
device_skey int8,
game_skey int8,
game_date_time timestamp,
opener_amount float8,
opener_adj_amount float8,
closer_amount float8,
closer_adj_amount float8,
fill_amount float8,
fill_adj_amount float8,
credit_amount float8,
credit_adj_amount float8,
need_amount float8,
estimated_drop_amount float8,
unposted_drop_amount float8,
posted_drop_amount float8,
posted_drop_other_amount float8,
posted_drop_other_adj_amount float8,
marker_amount float8,
marker_adj_amount float8,
table_jackpot_amount float8,
table_jackpot_adjustment_amount float8,
posted_drop_promochip_amount float8,
posted_drop_promo_adj_amount float8,
cash_drop_amount float8,
total_drop_amount float8,
table_win_amount float8,
hold_pct float8,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(100),
md5_hash varchar(100) )
PARTITIONED BY (
part_dt date
)
STORED AS PARQUET LOCATION 's3://vv-staging-emr-cluster/data/mart/hvtb_mart_fact_device_revenue';

create or replace view shipdw.device_revenue_fact as select * from hive_schema_stg.device_revenue_fact with no schema binding;