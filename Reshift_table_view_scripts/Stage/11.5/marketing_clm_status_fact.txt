drop table hive_schema_stg.marketing_clm_status_fact;
CREATE EXTERNAL TABLE hive_schema_stg.marketing_clm_status_fact(
  ga360_fullvisitorid varchar(50), 
  client_id int, 
  lead_id varchar(50), 
  clm_stage varchar(50), 
  clm_sub_stage varchar(50), 
  clm_as_of_datetime timestamp, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar(50), 
  md5_hash varchar(50))
PARTITIONED BY ( 
  part_dt date)
STORED AS PARQUET
LOCATION
  's3://vv-staging-emr-cluster/data/mart/hvtb_mart_fact_marketing_clm_status';

create or replace view as seaware.marketing_clm_status_fact select * from hive_schema_stg.marketing_clm_status_fact with no schema binding;