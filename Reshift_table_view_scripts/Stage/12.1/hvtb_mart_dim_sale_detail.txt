drop table hive_schema_stg.sale_detail_dim;
CREATE EXTERNAL TABLE `hive_schema_stg.sale_detail_dim`(
voyage_id	varchar(100),
sale_detail_skey	bigint,
sale_id	bigint,
sale_detail_src	varchar(10)	,
sale_operator_num	int,
receipt_number	int,
check_status	varchar(10),
sales_voyagenumber	varchar(10)	,
location	varchar(50)	,
line_type	varchar(100)	,
line_Status	varchar(10)	,
line_description	varchar(200),
src_deleted_flag	boolean,
load_dt timestamp ,
upd_dt timestamp ,
primaryhash text ,
md5_hash text ,
part_dt text )
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-staging-emr-cluster/data/mart/hvtb_mart_dim_sale_detail';
  
  CREATE OR REPLACE VIEW shipdw.sale_detail_dim
AS CREATE OR REPLACE VIEW shipdw.sale_detail_dim AS SELECT * FROM hive_schema_stg.sale_detail_dim with no schema binding;