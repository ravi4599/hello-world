drop table if exists hive_schema_stg.seaware_sail_cabin_reserve_dim; 
  CREATE EXTERNAL TABLE hive_schema_stg.seaware_sail_cabin_reserve_dim
(
  sail_cabin_reserve_id int, 
  rec_start_dttm timestamp, 
  rec_end_dttm timestamp, 
  CABIN_NUMBER varchar(10), 
  SRC_SAIL_ID int, 
  RESERVE_TYPE varchar(15), 
  RESERVE_COMMENTS varchar(255), 
  IS_RESERVE_ALLOWS_BOOKING varchar(1), 
  IS_RESERVE_RESTRICTED varchar(1), 
  IS_SAIL_RESERVE varchar(1), 
  RESERVE_TYPE_COMMENTS varchar(255), 
  md5_dim varchar(256), 
  etl_ld_status varchar(256), 
  etl_ld_dt timestamp, 
  etl_upd_dt timestamp
)
STORED AS PARQUET 
LOCATION
  's3://vv-staging-emr-cluster/data/core/seaware/hvtb_nbx_core_sw_sail_cabin_reserve_dim'
;