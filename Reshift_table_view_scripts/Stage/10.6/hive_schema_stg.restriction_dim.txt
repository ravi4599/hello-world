
drop table hive_schema_stg.restriction_dim

CREATE EXTERNAL TABLE hive_schema_stg.restriction_dim         
(restriction_skey bigint,
voyage_id varchar(100),
person_id integer,
restrictions_id integer,
restrictions_guid varchar(100),
restrictions_type varchar(10),
categoryid integer,
category_code varchar(20),
category varchar(512),
comment varchar(500),
from_date timestamp,
to_date timestamp,
deleted_flag boolean,
created_date timestamp,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar,
md5_hash varchar)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-staging-emr-cluster/data/mart/hvtb_mart_dim_restriction' 