CREATE EXTERNAL TABLE hive_schema_stg.ship_revenue_fact (voyage_id varchar(50),
sale_detail_skey bigint,
pos_item_skey bigint,
folio_item_skey bigint,
person_skey bigint,
ship_code varchar(10),
voyage_skey bigint,
itinerary_skey bigint,
outlet_skey bigint,
activity_skey bigint,
sale_date_skey bigint,
sale_time_skey bigint,
device_skey bigint,
venue_skey bigint,
charge_skey bigint,
wearable_skey bigint,
time_on_device_seconds int,
avg_wager_amount double,
casino_game_dph int,
theoritical_winnings double,
booking_reference_number varchar(50),
sale_quantity double,
item_list_price_amount double,
debit_amount double,
credit_amount double,
tax_amount double,
discount_amount double,
discount_value double,
cost_of_goods double,
manual_adjustment_amount double,
total_collected_sales_amount double,
total_collected_sales_ap double,
total_shared_revenue_amount double,
total_shared_revenue_ap double,
source_type varchar(50),
load_dt timestamp,
upd_dt timestamp,
part_dt date)
STORED AS PARQUET LOCATION 's3://vv-prod-emr-cluster/data/mart/hvtb_mart_fact_revenue';

CREATE OR REPLACE VIEW shipdw.ship_revenue_fact AS SELECT * FROM hive_schema_stg.ship_revenue_fact with no schema binding;