CREATE EXTERNAL TABLE hive_schema_stg.preference_dim
(preference_skey integer,
  voyage_id varchar(100),
  preference_id integer,    
  person_id integer,
  preference_category_code varchar(20),
  category varchar(512), 
  description varchar(300), 
  comment varchar(500), 
  allergy varchar(512),
  allergy_code varchar(20),
  preference_upd_date timestamp, 
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar, 
  md5_hash varchar
)
ROW FORMAT SERDE
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
STORED AS INPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_preference'