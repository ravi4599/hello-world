CREATE EXTERNAL TABLE hive_schema_stg.person_address_fact
(
   voyage_id varchar(100),
	person_skey bigint,
	address_skey bigint,
	src_deleted_flag boolean,
	load_dt timestamp,
	upd_dt timestamp,
	part_dt date
)
STORED AS PARQUET 
LOCATION
  's3://vv-prod-emr-cluster/data/mart/hvtb_mart_fact_person_address';


