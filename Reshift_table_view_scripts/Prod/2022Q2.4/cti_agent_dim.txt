
drop table hive_schema_stg.cti_agent_dim;
CREATE EXTERNAL TABLE hive_schema_stg.cti_agent_dim (
`cti_agent_skey` bigint,
`src_agent_id` bigint,
`cti_team_skey` bigint,
`agent_username` varchar(500),
`agent_first_name` varchar(500),
`agent_last_name` varchar(500),
`email_address` varchar(500),
`is_active_flg` boolean,
`is_supervisor_flg` boolean,
`profile_id` bigint,
`profile_name` varchar(500),
`location` varchar(500),
`etl_load_dt` timestamp,
`load_dt` timestamp,
`upd_dt` timestamp,
`primaryhash` varchar(500),
`md5_hash` varchar(500)
)
STORED AS PARQUET LOCATION 's3://vv-prod-emr-cluster/data/mart/hvtb_nbx_core_cti_agent_dim'';
