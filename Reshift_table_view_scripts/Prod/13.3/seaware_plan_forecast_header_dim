CREATE EXTERNAL TABLE hive_schema_stg.seaware_plan_forecast_header_dim(
plan_forecast_id bigint,
rec_start_dttm timestamp,
rec_end_dttm timestamp,
src_header_id int,
sail_id int,
package_type varchar(15),
cabin_ctg_generic varchar(15),
forecast_version_id int,
forecast_metric_id int,
comments varchar(255),
load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar(255), 
  md5_hash varchar(255))
STORED AS PARQUET
LOCATION
's3://vv-prod-emr-cluster/data/core/seaware/hvtb_nbx_core_sw_plan_forecast_header_dim'	

CREATE OR REPLACE VIEW seaware.seaware_plan_forecast_header_dim AS SELECT * FROM hive_schema_stg.seaware_plan_forecast_header_dim with no schema binding;
