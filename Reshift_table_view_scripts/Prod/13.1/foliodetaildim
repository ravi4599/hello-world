CREATE EXTERNAL TABLE hive_schema_stg.foliodetaildim(
voyage_id varchar(100),
folio_detail_skey bigint,
folio_detail_id bigint,
currency_code varchar(200),
location varchar(50),
line_type varchar(100),
line_Status varchar(10),
line_description varchar(200),
sale_comment varchar(240),
sale_discount_comment varchar(200),
src_deleted_flag boolean,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(50),
md5_hash varchar(50))
STORED AS PARQUET
LOCATION
's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_folio_detail'

---------------------------

CREATE OR REPLACE VIEW shipdw.foliodetaildim AS SELECT * FROM hive_schema_stg.foliodetaildim with no schema binding;
