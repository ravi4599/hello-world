============================================================================

drop table hive_schema_stg.device_dim

CREATE EXTERNAL TABLE hive_schema_stg.device_dim(
device_skey int8,
voyage_id varchar(100),
device_id numeric(10,0),
device_type_id numeric(10,0),
device_type_name varchar(100),
device_sub_type_name varchar(100),
device_machine_number varchar(100),
device_category_id numeric(3,0),
device_category_name varchar(100),
device_sub_category_id numeric(3,0),
device_sub_category_name varchar(100),
device_cab_type_id numeric(10,0),
device_cab_type_name varchar(100),
device_serial_number varchar(100),
device_zone_id numeric(10,0),
device_zone_name varchar(100),
bank_id numeric(10,0),
bank_name varchar(100),
bill_acceptor_flg boolean,
coin_acceptor_flg boolean,
lock_down_flg boolean,
video_poker_flg boolean,
shift_id numeric(1,0),
mix_type_id numeric(16,0),
mix_type_name varchar(100),
device_theme_name varchar(100),
manufacturer_id numeric(10,0),
manufacturer_name varchar(100),
game_site_id numeric(5,0),
game_site_name varchar(100),
active_flg boolean,
device_start_dt timestamp,
device_sold_Dt timestamp,
currency_name varchar(100),
device_mode_id numeric(4,0),
device_mode_name varchar(100),
device_operating_dt timestamp,
online_addr_id varchar(100),
online_mac_id varchar(100),
calc_package_name varchar(100),
calc_package_id numeric(3,0),
free_play_enabled_flg boolean,
cwa_enabled_flg boolean,
ship_code varchar(10),
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(100),
md5_hash varchar(100)
)
STORED AS PARQUET LOCATION 's3://vv-qa-emr-cluster/data/mart/hvtb_mart_dim_device';
====================================================================================================================================
create or replace view shipdw.device_dim as select * from hive_schema_stg.device_dim with no schema binding;