CREATE EXTERNAL TABLE hive_schema_stg.SEWARE_TRANSFER_REQUEST_POI_LKP
(
TRANSFER_REQUEST_POI_ID bigint,
POINT_OF_INTEREST varchar(100),
POI_ADDRESS varchar(500),
POI_CITY varchar(50),
POI_ZIP varchar(20),
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(20),
md5_hash varchar(20))
ROW FORMAT SERDE 
  'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe' 
STORED AS INPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat' 
OUTPUTFORMAT 
  'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION
  's3://vv-qa-emr-cluster/data/mart/hvtb_mart_lkp_sw_transfer_request';
  
  create or replace view seaware.SEWARE_TRANSFER_REQUEST_POI_LKP
as select * from  hive_schema_stg.SEWARE_TRANSFER_REQUEST_POI_LKP
with no schema binding;