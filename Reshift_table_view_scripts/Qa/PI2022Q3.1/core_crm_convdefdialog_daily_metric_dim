CREATE  EXTERNAL TABLE  hive_schema_stg.core_crm_convdefdialog_daily_metric(
convdefdialogdailymetric_skey bigint,
Id varchar(50),
Article varchar(255),
ArticleName varchar(255),
BotDefinitionName varchar(255),
ChannelType varchar(255),
ConversationDefinitionId varchar(50),
DialogDefinitionId varchar(50),
DialogDefinitionName varchar(255),
IntegrationType varchar(255),
IntegrationName varchar(128),
IsSuccessful boolean,
Language varchar(40),
MetricDateTime date,
MetricType varchar(255),
name varchar(255),
sessionendtype varchar(255),
sessiontransfertype varchar(255),
staticchoicevalue varchar(255),
value int,
createddate timestamp,
lastmodifieddate timestamp,
isdeleted boolean,
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar(255), 
  md5_hash varchar(255)
  )
  STORED AS PARQUET  
LOCATION
   's3://vv-qa-emr-cluster/data/mart/hvtb_nbx_mart_crm_convdefdialog_daily_metric';

Create view crm.core_crm_convdefdialog_daily_metric as select * from hive_schema_stg.core_crm_convdefdialog_daily_metric with no schema binding;
