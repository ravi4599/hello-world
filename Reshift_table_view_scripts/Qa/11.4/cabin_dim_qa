

CREATE EXTERNAL TABLE hive_schema_stg.cabin_dim(
voyage_id varchar(50),
cabin_skey int8, 
cabin_id int, 
cabin_number varchar(50), 
ship_code varchar(50),
cabin_floor_name varchar(50), 
cabin_category varchar(50), 
cabin_type varchar(50), 
cabin_view varchar(50),
cabin_category_generic varchar(50), 
src_deleted_flag boolean, 
load_dt timestamp, 
upd_dt timestamp, 
primaryhash varchar(50), 
md5_hash varchar(50))
STORED AS PARQUET
LOCATION
's3://vv-qa-emr-cluster/data/mart/hvtb_mart_dim_cabin'

CREATE OR REPLACE VIEW shipdw.cabin_dim AS SELECT * FROM hive_schema_stg.cabin_dim with no schema binding;