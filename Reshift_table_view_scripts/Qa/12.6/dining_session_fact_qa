CREATE TABLE hive_schema_stg.dining_session_fact (
voyage_id varchar(100),
voyage_skey bigint,
itinerary_skey bigint,
venue_skey bigint,
dining_session_skey bigint,
time_start_skey bigint,
time_end_skey bigint,
number_of_timeslots int,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(256),
md5_hash varchar(256),
part_dt date
)STORED AS PARQUET LOCATION 's3://vv-qa-emr-cluster/data/mart/hvtb_mart_fact_dining_session';

CREATE OR REPLACE VIEW shipdw.dining_session_fact as SELECT * FROM hive_schema_stg.dining_session_fact with no schema binding;

