CREATE EXTERNAL TABLE hive_schema_stg.menuitem_allergy_fact(
voyage_id varchar(50), 
menu_item_skey int8, 
allergy_skey int8, 
src_deleted_flg boolean, 
load_dt timestamp, 
upd_dt timestamp)
PARTITIONED BY ( 
part_dt date)
STORED AS PARQUET
LOCATION
's3://vv-qa-emr-cluster/data/mart/hvtb_mart_fact_menuitem_allergy'

CREATE OR REPLACE VIEW shipdw.menuitem_allergy_fact AS SELECT * FROM hive_schema_stg.menuitem_allergy_fact with no schema binding;