-- Drop table

-- DROP TABLE hive_schema_stg.postvoyage_survey_question_answer_dim;

CREATE TABLE hive_schema_stg.postvoyage_survey_question_answer_dim (
question_answer_id bigint, 
src_question_id varchar(50),
variable_id varchar(50),
question_text varchar(500),
Type varchar(50),
category varchar(50),
answer_code int,
answer_code_text varchar(500),
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(500),
md5_hash varchar(500)
);
