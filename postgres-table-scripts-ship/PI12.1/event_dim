CREATE TABLE hive_schema_stg.event_dim(event_skey int8, voyage_id text, event_id text, template_id text, event_name text, event_start_time timestamp, event_duration_minutes int4, event_date date, venue_skey int8, event_location text, event_capacity int4, event_inventoried_flg bool, event_hidden_flg bool, event_vip_flg bool, event_deleted_flg bool, load_dt timestamp, upd_dt timestamp, primaryhash text, md5_hash text, part_date date);

Create or replace view shipdw.event_dim as select * from hive_schema_stg.event_dim;
