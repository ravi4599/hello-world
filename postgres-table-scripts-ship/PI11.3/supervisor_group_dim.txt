
CREATE TABLE hive_schema_stg.supervisor_group_dim(
  voyage_id text,
  supervisor_group_skey long,
  supervisor_group_id long,
  supervisor_group_code text,
  supervisor_group_name text,
  src_active_flag bool,
  src_deleted_flag bool,
  load_dt timestamp,    
  upd_dt timestamp,
  primaryhash text,
  md5_hash text
)

CREATE OR REPLACE VIEW shipdw.supervisor_group_dim
AS 
SELECT * FROM hive_schema_stg.supervisor_group_dim;
