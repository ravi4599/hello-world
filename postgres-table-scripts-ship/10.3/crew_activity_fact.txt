-- hive_schema_stg.crew_activity_fact definition

-- Drop table

DROP TABLE hive_schema_stg.crew_activity_fact cascade;

CREATE TABLE hive_schema_stg.crew_activity_fact (
	voyage_id text NULL,
	ship_code text NULL,
	org_unit_skey int8 NULL,
	crew_activity_detail_skey int8 NULL,
	person_skey int8 NULL,
	is_actuals boolean NULL,
	activity_skey int4 NULL,
	schedule_activity_day date NULL,
	schedule_activity_datetime_from timestamp NULL,
	schedule_activity_datetime_to timestamp NULL,
	schedule_activity_duration_hours int4 NULL,
	is_deleted boolean NULL,
	is_actual_activity_confirmed boolean NULL,
	is_actual_activity_approved boolean NULL,
	load_dt timestamp NOT NULL,
	upd_dt timestamp NOT NULL,
	primaryhash text NULL,
	md5_hash text NULL,
	part_dt date NOT NULL
);

CREATE OR REPLACE VIEW shipdw.crew_activity_fact
AS SELECT * FROM hive_schema_stg.crew_activity_fact;