DROP TABLE hive_schema_stg.dining_reservation_preference_fact cascade;

CREATE TABLE hive_schema_stg.dining_reservation_preference_fact (
voyage_id varchar(100),
person_skey int4,
voyage_skey int4,
ship_code varchar(50),
dining_reservation_detail_skey int4,
dining_reservation_preference_skey int4,
preference_value varchar(1000),
load_dt	timestamp,
upd_dt timestamp,
primaryhash text,
md5_hash text,
part_dt date
);

CREATE OR REPLACE VIEW shipdw.dining_reservation_preference_fact
AS SELECT * FROM hive_schema_stg.dining_reservation_preference_fact;