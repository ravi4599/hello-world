CREATE TABLE hive_schema_stg.event_dim(event_skey integer, voyage_id text, event_id text, template_id text, event_name text, event_start_time timestamp, event_duration_minutes int, event_date date, integer bigint, event_location text, event_capacity int, event_inventoried_flg boolean, event_hidden_flg boolean, event_vip_flg boolean, event_deleted_flg text, load_dt timestamp, upd_dt timestamp,primaryhash text, md5_hash text)