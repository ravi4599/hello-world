CREATE TABLE hive_schema_stg.restriction_dim (
	restriction_skey int8 NOT NULL,
	voyage_id text NULL,
	person_id int8 NULL,
	restrictions_id int8 NULL,
	restrictions_guid text NULL,
	restrictions_type text NULL,
	categoryid int8 NULL,
	category_code text NULL,
	category text NULL,
	"comment" text NULL,
	from_date timestamp NULL,
	"to_date" timestamp NULL,
	deleted_flag bool NULL,
	created_date timestamp NULL,
	load_dt timestamp NULL,
	upd_dt timestamp NULL,
	primaryhash text NULL,
	md5_hash text NULL,
	CONSTRAINT restriction_dim_pkey PRIMARY KEY (restriction_skey)
);