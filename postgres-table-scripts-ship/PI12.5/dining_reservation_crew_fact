CREATE TABLE hive_schema_stg.dining_reservation_crew_fact(
voyage_id varchar(100),
voyage_skey int8,
dining_reservation_detail_skey int8,
served_by_skey int8,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(256), 
md5_hash varchar(256),
part_dt date);

CREATE OR REPLACE VIEW shipdw.dining_reservation_crew_fact AS SELECT * FROM hive_schema_stg.dining_reservation_crew_fact;