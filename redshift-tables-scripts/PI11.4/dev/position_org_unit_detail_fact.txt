drop table if exists hive_schema_stg.position_org_unit_detail_fact cascade;

CREATE EXTERNAL TABLE hive_schema_stg.position_org_unit_detail_fact (
	voyage_id text,
	position_skey bigint,
	org_unit_skey bigint,
	parent_position_skey bigint,
	parent_org_unit_skey bigint,
	position_hierarchy_level bigint,
	supervisor_group_skey bigint,
	src_active_flag	boolean,
	src_deleted_flag boolean,
	load_dt	timestamp,
	upd_dt timestamp,
	primaryhash text,
	md5_hash text,
	part_dt date)
STORED AS PARQUET   
LOCATION 's3://vv-dev-emr-cluster/data/mart/position_org_unit_detail_fact';

create or replace view shipdw.position_org_unit_detail_fact as select * from hive_schema_stg.position_org_unit_detail_fact with no schema binding;

GRANT USAGE ON SCHEMA shipdw TO  group Tableau_poweruser_group;

grant select on all tables in schema shipdw to group Tableau_poweruser_group ;


