CREATE TABLE IF NOT EXISTS seaware_stg.HBTB_NBX_SAILOR_NBX_ACTIVITY_MAPPING
(
   NBXUNIQUEKEY VARCHAR(256) NOT NULL DISTKEY,
   SAILORID     INTEGER SORTKEY,
   ACTIVITYID   INTEGER,
   CATEGORY     VARCHAR(30),
   DESCRIPTION  VARCHAR(500),
   RANKING      INTEGER, 
   START_DATETIME TIMESTAMP,
   END_DATETIME TIMESTAMP,
   ISACTIVE     CHAR(1),
   ISRECOMMENDED CHAR(1),
   TIMESTAMP TIMESTAMP,
   etl_ld_dt TIMESTAMP,
   etl_upd_dt TIMESTAMP
);