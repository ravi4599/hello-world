CREATE EXTERNAL TABLE hive_schema_stg.CRM_AGENCY_MASTER
(
   Agency_id		integer,
   REC_START_DTTM	timestamp ,
   REC_END_DTTM	        timestamp,
   Id			varchar(500),
   seaware_agency_id__c	varchar(100),
   name			varchar(500),
   lastname		varchar(500),
   firstname		varchar(500),
   billingcity		varchar(500),
   billingcountry	varchar(500),
   billingpostalcode	varchar(500),
   billingstate	 	varchar(500),
   billingstreet	varchar(500),
   md5_dim		varchar(500),
   etl_ld_status	varchar(20),
   etl_ld_dt 		timestamp,
   etl_upd_dt	        timestamp
)
stored as parquet 
LOCATION
  's3://vv-prod-emr-cluster/data/core/crm_kpi/hvtb_nbx_core_crm_agency_master_dim'
;

