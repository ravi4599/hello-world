
create or replace view seaware.seaware_sail_dim
as select * from hive_schema_stg.seaware_sail_dim
with no schema binding;

create or replace view seaware.seaware_agency_dim
as select * from hive_schema_stg.seaware_agency_dim
with no schema binding;


