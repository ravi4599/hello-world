CREATE EXTERNAL TABLE hive_schema_stg.position_dim(
 position_skey int8, 
  voyage_id varchar(50), 
  position_id int4, 
  position_code varchar(50),
  position_name varchar(50), 
  position_dept_id int4, 
  position_dept_name varchar(50), 
  src_active_flag boolean,
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar(50), 
  md5_hash varchar(50))
STORED AS PARQUET  
LOCATION
  's3://vv-qa-emr-cluster/data/mart/hvtb_mart_dim_position' 
  
CREATE OR REPLACE VIEW shipdw.position_dim AS SELECT * FROM hive_schema_stg.position_dim with no schema binding;