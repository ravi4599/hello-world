CREATE EXTERNAL TABLE hive_schema_stg.venue_dim(
    venue_skey int8, 
  voyage_id varchar(50), 
  venue_id varchar(50), 
  venue_name varchar(50), 
  venue_sector_name varchar(50), 
  data_src varchar(50), 
  density_threshold_1 int4,
  density_threshold_2 int4,
  load_dt timestamp, 
  upd_dt timestamp, 
  primaryhash varchar(50), 
  md5_hash varchar(50))
STORED AS PARQUET  
LOCATION
  's3://vv-qa-emr-cluster/data/mart/hvtb_mart_dim_venue' 
  
 CREATE OR REPLACE VIEW shipdw.venue_dim AS SELECT * FROM hive_schema_stg.venue_dim with no schema binding;