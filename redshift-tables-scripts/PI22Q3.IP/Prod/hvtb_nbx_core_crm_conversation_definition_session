CREATE EXTERNAL TABLE hive_schema_stg.hvtb_nbx_core_crm_conversation_definition_session(  convdefinition_session_skey bigint, 
id varchar(50),
ChannelType varchar(100),
ConversationDefinitionId varchar(50),
versionnumber int,
HasErrorLogs boolean,
IntegrationName varchar(128),
IntegrationType varchar(255),
Language varchar(40),
SessionDuration int,
SessionEndType varchar(255),
SessionStartTime timestamp,
SessionStatus varchar(255),
SessionTransferResult varchar(255),
SessionTransferTargetType varchar(255),
SessionTransferType varchar(255),
createddate timestamp,
lastmodifieddate timestamp,
isdeleted boolean,
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(100),
md5_hash varchar(100)
)
STORED AS PARQUET LOCATION 
  's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_conversation_definition_session';
  
  Create or Replace view crm.core_crm_conversation_definition_session as select * from hive_schema_stg.hvtb_nbx_core_crm_conversation_definition_session with no schema binding;
