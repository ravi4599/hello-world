CREATE EXTERNAL TABLE hive_schema_stg.SEWARE_GUEST_SW_VXP_REL(
SW_VXP_GUEST_REL_ID int4,
SRC_GUEST_ID int4,
CLIENT_ID int4,
SAIL_DATE_FROM timestamp,
SAIL_DATE_TO timestamp,
SRC_RES_ID int4,
VXP_GUEST_ID varchar(100),
VXP_RES_ID varchar(100),
VXP_RESERVATION_GUEST_ID varchar(100),
load_dt timestamp,
upd_dt timestamp,
primaryhash varchar(100),
md5_hash varchar(100))
STORED AS PARQUET LOCATION 's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_SEWARE_GUEST_SW_VXP_REL';

create or replace view seaware.seware_guest_sw_vxp_rel as select * from hive_schema_stg.seware_guest_sw_vxp_rel with no schema binding;
