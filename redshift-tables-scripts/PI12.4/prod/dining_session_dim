CREATE EXTERNAL TABLE hive_schema_stg.dining_session_dim(
voyage_id varchar(100),
dining_session_skey int8,
dining_session_id varchar(100),
dining_session_name varchar(100),
session_day_name varchar(100),
session_voyage_number varchar(100),
venue_id varchar(100),
timeslot_duration_seconds int,
max_party_size int,
load_dt timestamp, 
upd_dt timestamp, 
primaryhash varchar(100), 
md5_hash varchar(100))
STORED AS PARQUET
LOCATION
's3://vv-prod-emr-cluster/data/mart/hvtb_mart_dim_dining_session'