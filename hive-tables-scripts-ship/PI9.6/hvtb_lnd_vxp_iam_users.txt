CREATE EXTERNAL TABLE `shipdw`.`hvtb_lnd_vxp_iam_users`(`username` string, `password` string, `companyid` string, `useremail` string, `usertype` string, `accountnonexpired` boolean, `accountnonlocked` boolean, `credentialsnonexpired` boolean, `enabled` boolean, `pin` string, `usercontactnumber` string, `addeddate` timestamp, `addedby` string, `lastmodifieddate` timestamp, `lastmodifiedby` string, `ispasswordresetrequired` boolean, `userguid` string, `passwordresetexpirytime` timestamp, `passwordresetid` string, `emailverificationid` string, `emailverificationexpirytime` timestamp, `emailverificationstatus` string, `VoyageId` string, `BatchTime` timestamp)
PARTITIONED BY (`Part_Date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1',
  'timestamp.formats' = 'yyyy-MM-dd HH:mm:ss'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 'hdfs://DEV/data/apps/nbx/hive/landing/vxp/hvtb_lnd_vxp_iam_users'

spark.sql("create view shipdw.hvtb_parse_vxp_iam_users as select * from shipdw.hvtb_lnd_vxp_iam_users")