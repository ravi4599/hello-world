spark.sql("drop table shipdw.hvtb_lnd_vxp_dxp_voyage")

spark.sql("""CREATE EXTERNAL TABLE `shipdw`.`hvtb_lnd_vxp_dxp_voyage`(voyageid_src string,
	shipid string,
	number string,
	namekey string,
	embarkdate timestamp,
	debarkdate timestamp,
	embarkportcode string,
	debarkportcode string,
	sailingpackageid string,
	isactive boolean,
	isdeleted boolean,
	addeddate timestamp,
	addedby string,
	lastmodifieddate timestamp,
	lastmodifiedby string,
	sailingpackagecode string,
	shipcode_src string,
	guestcount int,
	boardingmethod string,
	isocienabled boolean,
	ismocienabled boolean,
	externalreferenceid string,`VoyageId` string, `BatchTime` timestamp, `ShipCode` string)
PARTITIONED BY (`Part_date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION '/data/apps/nbx/hive/landing/vxp/hvtb_lnd_vxp_dxp_voyage'""")