venue :-

spark.sql("create external table shipdw.hvtb_lnd_vxp_foodnbev_venue(venueid string,venuetypeid string,name string,logitude string,latitude string,address string,tagline string,radius decimal(5,2),addeddate timestamp,inactiveflag boolean,message string,countryid string,cityid string,phonenumber string,employeepinlength int,taxpercentage decimal(5,2),gratuitypercentage decimal(5,2),commisionpercentage decimal(5,2),timezone  string, currency string,servicechargepercentage decimal(5,2),currenttheme  string,environment  string,globalvenueid string,isfooddeliveryavailable boolean,operatorid string,machineid string, VoyageId string,BatchTime timestamp)partitioned by (Part_Date date) STORED AS parquet LOCATION '/data/apps/nbx/hive/landing/vxp/hvtb_lnd_vxp_foodnbev_venue'")                    

spark.sql("create view shipdw.hvtb_parse_vxp_foodnbev_venue as select * from shipdw.hvtb_lnd_vxp_foodnbev_venue")