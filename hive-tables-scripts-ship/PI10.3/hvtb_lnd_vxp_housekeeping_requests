spark.sql("drop table shipdw.hvtb_lnd_vxp_housekeeping_requests")

spark.sql("""CREATE EXTERNAL TABLE `shipdw`.`hvtb_lnd_vxp_housekeeping_requests`(`id` bigint, `public_id` string, `place_id` int, `description` string, `created` timestamp, `priority` int, `request_type` int, `place_deck_id` int, `place_section_id` int, `assigned` string, `created_by` string, `loyalty` int, `started_at` timestamp, `completed_at` timestamp, `assigned_at` timestamp, `at_risk_at` timestamp, `overdue_at` timestamp, `cancelled_at` timestamp, `come_back_later_at` timestamp, `come_back_later_by` string, `deleted` timestamp, `guests` string,`voyage_number` string, `VoyageId` string, `loadDate` timestamp, `BatchTime` timestamp,`ShipCode` string)
PARTITIONED BY (`Part_Date` date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
  'serialization.format' = '1'
)
STORED AS
  INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
  OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION '/data/apps/nbx/hive/landing/vxp/hvtb_lnd_vxp_housekeeping_requests' """)

spark.sql("drop view shipdw.hvtb_parse_vxp_housekeeping_requests")

spark.sql("Create view shipdw.hvtb_parse_vxp_housekeeping_requests as select * from shipdw.hvtb_lnd_vxp_housekeeping_requests")