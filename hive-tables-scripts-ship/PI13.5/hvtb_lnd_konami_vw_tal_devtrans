spark.sql("""select * from shipdw.hvtb_lnd_konami_vw_tal_devtrans""")
spark.sql(""" CREATE EXTERNAL TABLE shipdw.hvtb_lnd_konami_vw_tal_devtrans(
GAMEID decimal(10,0),
DTTYPID decimal(5,0),
CURID decimal(4,0),
VARIANCEAMT decimal(14,2),
COMPEXPIRE timestamp,
CREDITPOINT decimal(14,2),
PRODERRCODE string,
PPRLINENO decimal(4,0),
TRANSID decimal(16,0),
PRIORITY decimal(1,0),
DEVID decimal(10,0),
PLAYID decimal(10,0),
JACKPOTNAME string,
SEATNUM decimal(2,0),
PTNID decimal(10,0),
ACCTID decimal(10,0),
CARDID decimal(10,0),
PAPERREFNUM string,
CHECKNUM string,
CONTROLNUM decimal(16,0),
EVTID decimal(10,0),
DENOM decimal(17,5),
DEBITAMT decimal(14,2),
CREDITAMT decimal(14,2),
BALANCEAMT decimal(14,2),
COMPID decimal(10,0),
VOUCHERID decimal(16,0),
PCALCID decimal(2,0),
POINTRATIO decimal(10,6),
COMPPOINTRATIO decimal(10,6),
DEBITPOINT decimal(14,2),
DEBITCOMPPOINT decimal(14,2),
CREDITCOMPPOINT decimal(14,2),
DEBITBONUSPOINT decimal(14,2),
CREDITBONUSPOINT decimal(14,2),
GSTID decimal(5,0),
SHIFTID decimal(1,0),
YYYYINSERTED decimal(4,0),
MMINSERTED decimal(2,0),
DDINSERTED  decimal(2,0),
DATEINSERTED timestamp,
LASTMODDATE timestamp,
LASTMODBY string,
CREDITBONUSCOMPPOINT decimal(14,2),
DEBITTIERPOINT decimal(14,2),
CREDITTIERPOINT decimal(14,2),
VoyageId string,
BatchTime timestamp,
ShipCode string)
PARTITIONED BY (Part_Date date)
ROW FORMAT SERDE 'org.apache.hadoop.hive.ql.io.parquet.serde.ParquetHiveSerDe'
WITH SERDEPROPERTIES (
'serialization.format' = '1'
)
STORED AS
INPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetInputFormat'
OUTPUTFORMAT 'org.apache.hadoop.hive.ql.io.parquet.MapredParquetOutputFormat'
LOCATION 'data/apps/nbx/hive/landing/vxp/hvtb_lnd_konami_vw_tal_devtrans' """)
